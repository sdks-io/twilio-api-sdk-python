"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper
from twilioapis.models.meta import Meta
from twilioapis.models.task_channel import TaskChannel


class ListTaskChannelResponse(object):
    """Implementation of the 'ListTaskChannelResponse' model.

    Attributes:
        channels (List[TaskChannel]): The model property of type List[TaskChannel].
        meta (Meta): The model property of type Meta.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "channels": "channels",
        "meta": "meta",
    }

    _optionals = [
        "channels",
        "meta",
    ]

    def __init__(
        self,
        channels=APIHelper.SKIP,
        meta=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ListTaskChannelResponse instance."""
        # Initialize members of the class
        if channels is not APIHelper.SKIP:
            self.channels = channels
        if meta is not APIHelper.SKIP:
            self.meta = meta

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        channels = None
        if dictionary.get("channels") is not None:
            channels = [
                TaskChannel.from_dictionary(x)
                    for x in dictionary.get("channels")
            ]
        else:
            channels = APIHelper.SKIP
        meta =\
            Meta.from_dictionary(
                dictionary.get("meta"))\
                if "meta" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(channels,
                   meta,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _channels=(
            self.channels
            if hasattr(self, "channels")
            else None
        )
        _meta=(
            self.meta
            if hasattr(self, "meta")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"channels={_channels!r}, "
            f"meta={_meta!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _channels=(
            self.channels
            if hasattr(self, "channels")
            else None
        )
        _meta=(
            self.meta
            if hasattr(self, "meta")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"channels={_channels!s}, "
            f"meta={_meta!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
