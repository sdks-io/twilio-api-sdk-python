"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper


class Meta(object):
    """Implementation of the 'Meta' model.

    Attributes:
        first_page_url (str): The model property of type str.
        key (str): The model property of type str.
        next_page_url (str): The model property of type str.
        page (int): The model property of type int.
        page_size (int): The model property of type int.
        previous_page_url (str): The model property of type str.
        url (str): The model property of type str.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "first_page_url": "first_page_url",
        "key": "key",
        "next_page_url": "next_page_url",
        "page": "page",
        "page_size": "page_size",
        "previous_page_url": "previous_page_url",
        "url": "url",
    }

    _optionals = [
        "first_page_url",
        "key",
        "next_page_url",
        "page",
        "page_size",
        "previous_page_url",
        "url",
    ]

    _nullables = [
        "next_page_url",
        "previous_page_url",
    ]

    def __init__(
        self,
        first_page_url=APIHelper.SKIP,
        key=APIHelper.SKIP,
        next_page_url=APIHelper.SKIP,
        page=APIHelper.SKIP,
        page_size=APIHelper.SKIP,
        previous_page_url=APIHelper.SKIP,
        url=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Meta instance."""
        # Initialize members of the class
        if first_page_url is not APIHelper.SKIP:
            self.first_page_url = first_page_url
        if key is not APIHelper.SKIP:
            self.key = key
        if next_page_url is not APIHelper.SKIP:
            self.next_page_url = next_page_url
        if page is not APIHelper.SKIP:
            self.page = page
        if page_size is not APIHelper.SKIP:
            self.page_size = page_size
        if previous_page_url is not APIHelper.SKIP:
            self.previous_page_url = previous_page_url
        if url is not APIHelper.SKIP:
            self.url = url

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        first_page_url =\
            dictionary.get("first_page_url")\
            if dictionary.get("first_page_url")\
                else APIHelper.SKIP
        key =\
            dictionary.get("key")\
            if dictionary.get("key")\
                else APIHelper.SKIP
        next_page_url =\
            dictionary.get("next_page_url")\
            if "next_page_url" in dictionary.keys()\
                else APIHelper.SKIP
        page =\
            dictionary.get("page")\
            if dictionary.get("page")\
                else APIHelper.SKIP
        page_size =\
            dictionary.get("page_size")\
            if dictionary.get("page_size")\
                else APIHelper.SKIP
        previous_page_url =\
            dictionary.get("previous_page_url")\
            if "previous_page_url" in dictionary.keys()\
                else APIHelper.SKIP
        url =\
            dictionary.get("url")\
            if dictionary.get("url")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(first_page_url,
                   key,
                   next_page_url,
                   page,
                   page_size,
                   previous_page_url,
                   url,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _first_page_url=(
            self.first_page_url
            if hasattr(self, "first_page_url")
            else None
        )
        _key=(
            self.key
            if hasattr(self, "key")
            else None
        )
        _next_page_url=(
            self.next_page_url
            if hasattr(self, "next_page_url")
            else None
        )
        _page=(
            self.page
            if hasattr(self, "page")
            else None
        )
        _page_size=(
            self.page_size
            if hasattr(self, "page_size")
            else None
        )
        _previous_page_url=(
            self.previous_page_url
            if hasattr(self, "previous_page_url")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"first_page_url={_first_page_url!r}, "
            f"key={_key!r}, "
            f"next_page_url={_next_page_url!r}, "
            f"page={_page!r}, "
            f"page_size={_page_size!r}, "
            f"previous_page_url={_previous_page_url!r}, "
            f"url={_url!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _first_page_url=(
            self.first_page_url
            if hasattr(self, "first_page_url")
            else None
        )
        _key=(
            self.key
            if hasattr(self, "key")
            else None
        )
        _next_page_url=(
            self.next_page_url
            if hasattr(self, "next_page_url")
            else None
        )
        _page=(
            self.page
            if hasattr(self, "page")
            else None
        )
        _page_size=(
            self.page_size
            if hasattr(self, "page_size")
            else None
        )
        _previous_page_url=(
            self.previous_page_url
            if hasattr(self, "previous_page_url")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"first_page_url={_first_page_url!s}, "
            f"key={_key!s}, "
            f"next_page_url={_next_page_url!s}, "
            f"page={_page!s}, "
            f"page_size={_page_size!s}, "
            f"previous_page_url={_previous_page_url!s}, "
            f"url={_url!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
