"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper


class Binding(object):
    """Implementation of the 'Binding' model.

    Attributes:
        sid (str): The unique string that we created to identify the Binding resource.
        account_sid (str): The SID of the
            [Account](https://www.twilio.com/docs/iam/api/account) that created the
            Binding resource.
        service_sid (str): The SID of the
            [Service](https://www.twilio.com/docs/notify/api/service-resource) the
            resource is associated with.
        credential_sid (str): The SID of the
            [Credential](https://www.twilio.com/docs/notify/api/credential-resource)
            resource to be used to send notifications to this Binding. If present,
            this overrides the Credential specified in the Service resource.
            Applicable only to `apn`, `fcm`, and `gcm` type Bindings.
        date_created (datetime): The date and time in GMT when the resource was
            created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        date_updated (datetime): The date and time in GMT when the resource was last
            updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        notification_protocol_version (str): The protocol version to use to send the
            notification. This defaults to the value of
            `default_xxxx_notification_protocol_version` in the
            [Service](https://www.twilio.com/docs/notify/api/service-resource) for
            the protocol. The current version is `"3"` for `apn`, `fcm`, and `gcm`
            type Bindings. The parameter is not applicable to `sms` and
            `facebook-messenger` type Bindings as the data format is fixed.
        endpoint (str): Deprecated.
        identity (str): The `identity` value that uniquely identifies the resource's
            [User](https://www.twilio.com/docs/chat/rest/user-resource) within the
            [Service](https://www.twilio.com/docs/notify/api/service-resource). Up to
            20 Bindings can be created for the same Identity in a given Service.
        binding_type (str): The transport technology to use for the Binding. Can be:
            `apn`, `fcm`, `gcm`, `sms`, or `facebook-messenger`.
        address (str): The channel-specific address. For APNS, the device token. For
            FCM and GCM, the registration token. For SMS, a phone number in E.164
            format. For Facebook Messenger, the Messenger ID of the user or a phone
            number in E.164 format.
        tags (List[str]): The list of tags associated with this Binding. Tags can be
            used to select the Bindings to use when sending a notification. Maximum
            20 tags are allowed.
        url (str): The absolute URL of the Binding resource.
        links (Any): The URLs of related resources.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sid": "sid",
        "account_sid": "account_sid",
        "service_sid": "service_sid",
        "credential_sid": "credential_sid",
        "date_created": "date_created",
        "date_updated": "date_updated",
        "notification_protocol_version": "notification_protocol_version",
        "endpoint": "endpoint",
        "identity": "identity",
        "binding_type": "binding_type",
        "address": "address",
        "tags": "tags",
        "url": "url",
        "links": "links",
    }

    _optionals = [
        "sid",
        "account_sid",
        "service_sid",
        "credential_sid",
        "date_created",
        "date_updated",
        "notification_protocol_version",
        "endpoint",
        "identity",
        "binding_type",
        "address",
        "tags",
        "url",
        "links",
    ]

    _nullables = [
        "sid",
        "account_sid",
        "service_sid",
        "credential_sid",
        "date_created",
        "date_updated",
        "notification_protocol_version",
        "endpoint",
        "identity",
        "binding_type",
        "address",
        "tags",
        "url",
        "links",
    ]

    def __init__(
        self,
        sid=APIHelper.SKIP,
        account_sid=APIHelper.SKIP,
        service_sid=APIHelper.SKIP,
        credential_sid=APIHelper.SKIP,
        date_created=APIHelper.SKIP,
        date_updated=APIHelper.SKIP,
        notification_protocol_version=APIHelper.SKIP,
        endpoint=APIHelper.SKIP,
        identity=APIHelper.SKIP,
        binding_type=APIHelper.SKIP,
        address=APIHelper.SKIP,
        tags=APIHelper.SKIP,
        url=APIHelper.SKIP,
        links=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Binding instance."""
        # Initialize members of the class
        if sid is not APIHelper.SKIP:
            self.sid = sid
        if account_sid is not APIHelper.SKIP:
            self.account_sid = account_sid
        if service_sid is not APIHelper.SKIP:
            self.service_sid = service_sid
        if credential_sid is not APIHelper.SKIP:
            self.credential_sid = credential_sid
        if date_created is not APIHelper.SKIP:
            self.date_created =\
                 APIHelper.apply_datetime_converter(
                date_created, APIHelper.RFC3339DateTime)\
                 if date_created else None
        if date_updated is not APIHelper.SKIP:
            self.date_updated =\
                 APIHelper.apply_datetime_converter(
                date_updated, APIHelper.RFC3339DateTime)\
                 if date_updated else None
        if notification_protocol_version is not APIHelper.SKIP:
            self.notification_protocol_version = notification_protocol_version
        if endpoint is not APIHelper.SKIP:
            self.endpoint = endpoint
        if identity is not APIHelper.SKIP:
            self.identity = identity
        if binding_type is not APIHelper.SKIP:
            self.binding_type = binding_type
        if address is not APIHelper.SKIP:
            self.address = address
        if tags is not APIHelper.SKIP:
            self.tags = tags
        if url is not APIHelper.SKIP:
            self.url = url
        if links is not APIHelper.SKIP:
            self.links = links

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sid =\
            dictionary.get("sid")\
            if "sid" in dictionary.keys()\
                else APIHelper.SKIP
        account_sid =\
            dictionary.get("account_sid")\
            if "account_sid" in dictionary.keys()\
                else APIHelper.SKIP
        service_sid =\
            dictionary.get("service_sid")\
            if "service_sid" in dictionary.keys()\
                else APIHelper.SKIP
        credential_sid =\
            dictionary.get("credential_sid")\
            if "credential_sid" in dictionary.keys()\
                else APIHelper.SKIP
        if "date_created" in dictionary.keys():
            date_created = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_created")).datetime\
                if dictionary.get("date_created") else None

        else:
            date_created = APIHelper.SKIP
        if "date_updated" in dictionary.keys():
            date_updated = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_updated")).datetime\
                if dictionary.get("date_updated") else None

        else:
            date_updated = APIHelper.SKIP
        notification_protocol_version =\
            dictionary.get("notification_protocol_version")\
            if "notification_protocol_version" in dictionary.keys()\
                else APIHelper.SKIP
        endpoint =\
            dictionary.get("endpoint")\
            if "endpoint" in dictionary.keys()\
                else APIHelper.SKIP
        identity =\
            dictionary.get("identity")\
            if "identity" in dictionary.keys()\
                else APIHelper.SKIP
        binding_type =\
            dictionary.get("binding_type")\
            if "binding_type" in dictionary.keys()\
                else APIHelper.SKIP
        address =\
            dictionary.get("address")\
            if "address" in dictionary.keys()\
                else APIHelper.SKIP
        tags =\
            dictionary.get("tags")\
            if "tags" in dictionary.keys()\
                else APIHelper.SKIP
        url =\
            dictionary.get("url")\
            if "url" in dictionary.keys()\
                else APIHelper.SKIP
        links =\
            dictionary.get("links")\
            if "links" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(sid,
                   account_sid,
                   service_sid,
                   credential_sid,
                   date_created,
                   date_updated,
                   notification_protocol_version,
                   endpoint,
                   identity,
                   binding_type,
                   address,
                   tags,
                   url,
                   links,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _service_sid=(
            self.service_sid
            if hasattr(self, "service_sid")
            else None
        )
        _credential_sid=(
            self.credential_sid
            if hasattr(self, "credential_sid")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _date_updated=(
            self.date_updated
            if hasattr(self, "date_updated")
            else None
        )
        _notification_protocol_version=(
            self.notification_protocol_version
            if hasattr(self, "notification_protocol_version")
            else None
        )
        _endpoint=(
            self.endpoint
            if hasattr(self, "endpoint")
            else None
        )
        _identity=(
            self.identity
            if hasattr(self, "identity")
            else None
        )
        _binding_type=(
            self.binding_type
            if hasattr(self, "binding_type")
            else None
        )
        _address=(
            self.address
            if hasattr(self, "address")
            else None
        )
        _tags=(
            self.tags
            if hasattr(self, "tags")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _links=(
            self.links
            if hasattr(self, "links")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!r}, "
            f"account_sid={_account_sid!r}, "
            f"service_sid={_service_sid!r}, "
            f"credential_sid={_credential_sid!r}, "
            f"date_created={_date_created!r}, "
            f"date_updated={_date_updated!r}, "
            f"notification_protocol_version={_notification_protocol_version!r}, "
            f"endpoint={_endpoint!r}, "
            f"identity={_identity!r}, "
            f"binding_type={_binding_type!r}, "
            f"address={_address!r}, "
            f"tags={_tags!r}, "
            f"url={_url!r}, "
            f"links={_links!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _service_sid=(
            self.service_sid
            if hasattr(self, "service_sid")
            else None
        )
        _credential_sid=(
            self.credential_sid
            if hasattr(self, "credential_sid")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _date_updated=(
            self.date_updated
            if hasattr(self, "date_updated")
            else None
        )
        _notification_protocol_version=(
            self.notification_protocol_version
            if hasattr(self, "notification_protocol_version")
            else None
        )
        _endpoint=(
            self.endpoint
            if hasattr(self, "endpoint")
            else None
        )
        _identity=(
            self.identity
            if hasattr(self, "identity")
            else None
        )
        _binding_type=(
            self.binding_type
            if hasattr(self, "binding_type")
            else None
        )
        _address=(
            self.address
            if hasattr(self, "address")
            else None
        )
        _tags=(
            self.tags
            if hasattr(self, "tags")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _links=(
            self.links
            if hasattr(self, "links")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!s}, "
            f"account_sid={_account_sid!s}, "
            f"service_sid={_service_sid!s}, "
            f"credential_sid={_credential_sid!s}, "
            f"date_created={_date_created!s}, "
            f"date_updated={_date_updated!s}, "
            f"notification_protocol_version={_notification_protocol_version!s}, "
            f"endpoint={_endpoint!s}, "
            f"identity={_identity!s}, "
            f"binding_type={_binding_type!s}, "
            f"address={_address!s}, "
            f"tags={_tags!s}, "
            f"url={_url!s}, "
            f"links={_links!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
