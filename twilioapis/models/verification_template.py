"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper


class VerificationTemplate(object):
    """Implementation of the 'VerificationTemplate' model.

    Attributes:
        sid (str): A 34 character string that uniquely identifies a Verification
            Template.
        account_sid (str): The unique SID identifier of the Account.
        friendly_name (str): A descriptive string that you create to describe a
            Template. It can be up to 32 characters long.
        channels (List[str]): A list of channels that support the Template. Can
            include: sms, voice.
        translations (Any): An object that contains the different translations of the
            template. Every translation is identified by the language short name and
            contains its respective information as the approval status, text and
            created/modified date.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sid": "sid",
        "account_sid": "account_sid",
        "friendly_name": "friendly_name",
        "channels": "channels",
        "translations": "translations",
    }

    _optionals = [
        "sid",
        "account_sid",
        "friendly_name",
        "channels",
        "translations",
    ]

    _nullables = [
        "sid",
        "account_sid",
        "friendly_name",
        "channels",
        "translations",
    ]

    def __init__(
        self,
        sid=APIHelper.SKIP,
        account_sid=APIHelper.SKIP,
        friendly_name=APIHelper.SKIP,
        channels=APIHelper.SKIP,
        translations=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a VerificationTemplate instance."""
        # Initialize members of the class
        if sid is not APIHelper.SKIP:
            self.sid = sid
        if account_sid is not APIHelper.SKIP:
            self.account_sid = account_sid
        if friendly_name is not APIHelper.SKIP:
            self.friendly_name = friendly_name
        if channels is not APIHelper.SKIP:
            self.channels = channels
        if translations is not APIHelper.SKIP:
            self.translations = translations

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sid =\
            dictionary.get("sid")\
            if "sid" in dictionary.keys()\
                else APIHelper.SKIP
        account_sid =\
            dictionary.get("account_sid")\
            if "account_sid" in dictionary.keys()\
                else APIHelper.SKIP
        friendly_name =\
            dictionary.get("friendly_name")\
            if "friendly_name" in dictionary.keys()\
                else APIHelper.SKIP
        channels =\
            dictionary.get("channels")\
            if "channels" in dictionary.keys()\
                else APIHelper.SKIP
        translations =\
            dictionary.get("translations")\
            if "translations" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(sid,
                   account_sid,
                   friendly_name,
                   channels,
                   translations,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _friendly_name=(
            self.friendly_name
            if hasattr(self, "friendly_name")
            else None
        )
        _channels=(
            self.channels
            if hasattr(self, "channels")
            else None
        )
        _translations=(
            self.translations
            if hasattr(self, "translations")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!r}, "
            f"account_sid={_account_sid!r}, "
            f"friendly_name={_friendly_name!r}, "
            f"channels={_channels!r}, "
            f"translations={_translations!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _friendly_name=(
            self.friendly_name
            if hasattr(self, "friendly_name")
            else None
        )
        _channels=(
            self.channels
            if hasattr(self, "channels")
            else None
        )
        _translations=(
            self.translations
            if hasattr(self, "translations")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!s}, "
            f"account_sid={_account_sid!s}, "
            f"friendly_name={_friendly_name!s}, "
            f"channels={_channels!s}, "
            f"translations={_translations!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
