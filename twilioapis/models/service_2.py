"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper


class Service2(object):
    """Implementation of the 'Service2' model.

    Attributes:
        sid (str): The unique string that we created to identify the Service resource.
        account_sid (str): The SID of the
            [Account](https://www.twilio.com/docs/iam/api/account) that created the
            Service resource.
        friendly_name (str): The name that appears in the body of your verification
            messages. It can be up to 30 characters long and can include letters,
            numbers, spaces, dashes, underscores. Phone numbers, special characters
            or links are NOT allowed. It cannot contain more than 4 (consecutive or
            non-consecutive) digits. **This value should not contain PII.**
        code_length (int): The length of the verification code to generate.
        lookup_enabled (bool): Whether to perform a lookup with each verification
            started and return info about the phone number.
        psd_2_enabled (bool): Whether to pass PSD2 transaction parameters when
            starting a verification.
        skip_sms_to_landlines (bool): Whether to skip sending SMS verifications to
            landlines. Requires `lookup_enabled`.
        dtmf_input_required (bool): Whether to ask the user to press a number before
            delivering the verify code in a phone call.
        tts_name (str): The name of an alternative text-to-speech service to use in
            phone calls. Applies only to TTS languages.
        do_not_share_warning_enabled (bool): Whether to add a security warning at the
            end of an SMS verification body. Disabled by default and applies only to
            SMS. Example SMS body: `Your AppName verification code is: 1234. Don’t
            share this code with anyone; our employees will never ask for the code`
        custom_code_enabled (bool): Whether to allow sending verifications with a
            custom code instead of a randomly generated one.
        push (Any): Configurations for the Push factors (channel) created under this
            Service.
        totp (Any): Configurations for the TOTP factors (channel) created under this
            Service.
        default_template_sid (str): The model property of type str.
        whatsapp (Any): The model property of type Any.
        passkeys (Any): The model property of type Any.
        verify_event_subscription_enabled (bool): Whether to allow verifications from
            the service to reach the stream-events sinks if configured
        date_created (datetime): The date and time in GMT when the resource was
            created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        date_updated (datetime): The date and time in GMT when the resource was last
            updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        url (str): The absolute URL of the resource.
        links (Any): The URLs of related resources.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sid": "sid",
        "account_sid": "account_sid",
        "friendly_name": "friendly_name",
        "code_length": "code_length",
        "lookup_enabled": "lookup_enabled",
        "psd_2_enabled": "psd2_enabled",
        "skip_sms_to_landlines": "skip_sms_to_landlines",
        "dtmf_input_required": "dtmf_input_required",
        "tts_name": "tts_name",
        "do_not_share_warning_enabled": "do_not_share_warning_enabled",
        "custom_code_enabled": "custom_code_enabled",
        "push": "push",
        "totp": "totp",
        "default_template_sid": "default_template_sid",
        "whatsapp": "whatsapp",
        "passkeys": "passkeys",
        "verify_event_subscription_enabled": "verify_event_subscription_enabled",
        "date_created": "date_created",
        "date_updated": "date_updated",
        "url": "url",
        "links": "links",
    }

    _optionals = [
        "sid",
        "account_sid",
        "friendly_name",
        "code_length",
        "lookup_enabled",
        "psd_2_enabled",
        "skip_sms_to_landlines",
        "dtmf_input_required",
        "tts_name",
        "do_not_share_warning_enabled",
        "custom_code_enabled",
        "push",
        "totp",
        "default_template_sid",
        "whatsapp",
        "passkeys",
        "verify_event_subscription_enabled",
        "date_created",
        "date_updated",
        "url",
        "links",
    ]

    _nullables = [
        "sid",
        "account_sid",
        "friendly_name",
        "lookup_enabled",
        "psd_2_enabled",
        "skip_sms_to_landlines",
        "dtmf_input_required",
        "tts_name",
        "do_not_share_warning_enabled",
        "custom_code_enabled",
        "push",
        "totp",
        "default_template_sid",
        "whatsapp",
        "passkeys",
        "verify_event_subscription_enabled",
        "date_created",
        "date_updated",
        "url",
        "links",
    ]

    def __init__(
        self,
        sid=APIHelper.SKIP,
        account_sid=APIHelper.SKIP,
        friendly_name=APIHelper.SKIP,
        code_length=0,
        lookup_enabled=APIHelper.SKIP,
        psd_2_enabled=APIHelper.SKIP,
        skip_sms_to_landlines=APIHelper.SKIP,
        dtmf_input_required=APIHelper.SKIP,
        tts_name=APIHelper.SKIP,
        do_not_share_warning_enabled=APIHelper.SKIP,
        custom_code_enabled=APIHelper.SKIP,
        push=APIHelper.SKIP,
        totp=APIHelper.SKIP,
        default_template_sid=APIHelper.SKIP,
        whatsapp=APIHelper.SKIP,
        passkeys=APIHelper.SKIP,
        verify_event_subscription_enabled=APIHelper.SKIP,
        date_created=APIHelper.SKIP,
        date_updated=APIHelper.SKIP,
        url=APIHelper.SKIP,
        links=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Service2 instance."""
        # Initialize members of the class
        if sid is not APIHelper.SKIP:
            self.sid = sid
        if account_sid is not APIHelper.SKIP:
            self.account_sid = account_sid
        if friendly_name is not APIHelper.SKIP:
            self.friendly_name = friendly_name
        self.code_length = code_length
        if lookup_enabled is not APIHelper.SKIP:
            self.lookup_enabled = lookup_enabled
        if psd_2_enabled is not APIHelper.SKIP:
            self.psd_2_enabled = psd_2_enabled
        if skip_sms_to_landlines is not APIHelper.SKIP:
            self.skip_sms_to_landlines = skip_sms_to_landlines
        if dtmf_input_required is not APIHelper.SKIP:
            self.dtmf_input_required = dtmf_input_required
        if tts_name is not APIHelper.SKIP:
            self.tts_name = tts_name
        if do_not_share_warning_enabled is not APIHelper.SKIP:
            self.do_not_share_warning_enabled = do_not_share_warning_enabled
        if custom_code_enabled is not APIHelper.SKIP:
            self.custom_code_enabled = custom_code_enabled
        if push is not APIHelper.SKIP:
            self.push = push
        if totp is not APIHelper.SKIP:
            self.totp = totp
        if default_template_sid is not APIHelper.SKIP:
            self.default_template_sid = default_template_sid
        if whatsapp is not APIHelper.SKIP:
            self.whatsapp = whatsapp
        if passkeys is not APIHelper.SKIP:
            self.passkeys = passkeys
        if verify_event_subscription_enabled is not APIHelper.SKIP:
            self.verify_event_subscription_enabled =\
                 verify_event_subscription_enabled
        if date_created is not APIHelper.SKIP:
            self.date_created =\
                 APIHelper.apply_datetime_converter(
                date_created, APIHelper.RFC3339DateTime)\
                 if date_created else None
        if date_updated is not APIHelper.SKIP:
            self.date_updated =\
                 APIHelper.apply_datetime_converter(
                date_updated, APIHelper.RFC3339DateTime)\
                 if date_updated else None
        if url is not APIHelper.SKIP:
            self.url = url
        if links is not APIHelper.SKIP:
            self.links = links

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sid =\
            dictionary.get("sid")\
            if "sid" in dictionary.keys()\
                else APIHelper.SKIP
        account_sid =\
            dictionary.get("account_sid")\
            if "account_sid" in dictionary.keys()\
                else APIHelper.SKIP
        friendly_name =\
            dictionary.get("friendly_name")\
            if "friendly_name" in dictionary.keys()\
                else APIHelper.SKIP
        code_length =\
            dictionary.get("code_length")\
            if dictionary.get("code_length")\
                else 0
        lookup_enabled =\
            dictionary.get("lookup_enabled")\
            if "lookup_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        psd_2_enabled =\
            dictionary.get("psd2_enabled")\
            if "psd2_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        skip_sms_to_landlines =\
            dictionary.get("skip_sms_to_landlines")\
            if "skip_sms_to_landlines" in dictionary.keys()\
                else APIHelper.SKIP
        dtmf_input_required =\
            dictionary.get("dtmf_input_required")\
            if "dtmf_input_required" in dictionary.keys()\
                else APIHelper.SKIP
        tts_name =\
            dictionary.get("tts_name")\
            if "tts_name" in dictionary.keys()\
                else APIHelper.SKIP
        do_not_share_warning_enabled =\
            dictionary.get("do_not_share_warning_enabled")\
            if "do_not_share_warning_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        custom_code_enabled =\
            dictionary.get("custom_code_enabled")\
            if "custom_code_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        push =\
            dictionary.get("push")\
            if "push" in dictionary.keys()\
                else APIHelper.SKIP
        totp =\
            dictionary.get("totp")\
            if "totp" in dictionary.keys()\
                else APIHelper.SKIP
        default_template_sid =\
            dictionary.get("default_template_sid")\
            if "default_template_sid" in dictionary.keys()\
                else APIHelper.SKIP
        whatsapp =\
            dictionary.get("whatsapp")\
            if "whatsapp" in dictionary.keys()\
                else APIHelper.SKIP
        passkeys =\
            dictionary.get("passkeys")\
            if "passkeys" in dictionary.keys()\
                else APIHelper.SKIP
        verify_event_subscription_enabled =\
            dictionary.get("verify_event_subscription_enabled")\
            if "verify_event_subscription_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        if "date_created" in dictionary.keys():
            date_created = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_created")).datetime\
                if dictionary.get("date_created") else None

        else:
            date_created = APIHelper.SKIP
        if "date_updated" in dictionary.keys():
            date_updated = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_updated")).datetime\
                if dictionary.get("date_updated") else None

        else:
            date_updated = APIHelper.SKIP
        url =\
            dictionary.get("url")\
            if "url" in dictionary.keys()\
                else APIHelper.SKIP
        links =\
            dictionary.get("links")\
            if "links" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(sid,
                   account_sid,
                   friendly_name,
                   code_length,
                   lookup_enabled,
                   psd_2_enabled,
                   skip_sms_to_landlines,
                   dtmf_input_required,
                   tts_name,
                   do_not_share_warning_enabled,
                   custom_code_enabled,
                   push,
                   totp,
                   default_template_sid,
                   whatsapp,
                   passkeys,
                   verify_event_subscription_enabled,
                   date_created,
                   date_updated,
                   url,
                   links,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _friendly_name=(
            self.friendly_name
            if hasattr(self, "friendly_name")
            else None
        )
        _code_length=(
            self.code_length
            if hasattr(self, "code_length")
            else None
        )
        _lookup_enabled=(
            self.lookup_enabled
            if hasattr(self, "lookup_enabled")
            else None
        )
        _psd_2_enabled=(
            self.psd_2_enabled
            if hasattr(self, "psd_2_enabled")
            else None
        )
        _skip_sms_to_landlines=(
            self.skip_sms_to_landlines
            if hasattr(self, "skip_sms_to_landlines")
            else None
        )
        _dtmf_input_required=(
            self.dtmf_input_required
            if hasattr(self, "dtmf_input_required")
            else None
        )
        _tts_name=(
            self.tts_name
            if hasattr(self, "tts_name")
            else None
        )
        _do_not_share_warning_enabled=(
            self.do_not_share_warning_enabled
            if hasattr(self, "do_not_share_warning_enabled")
            else None
        )
        _custom_code_enabled=(
            self.custom_code_enabled
            if hasattr(self, "custom_code_enabled")
            else None
        )
        _push=(
            self.push
            if hasattr(self, "push")
            else None
        )
        _totp=(
            self.totp
            if hasattr(self, "totp")
            else None
        )
        _default_template_sid=(
            self.default_template_sid
            if hasattr(self, "default_template_sid")
            else None
        )
        _whatsapp=(
            self.whatsapp
            if hasattr(self, "whatsapp")
            else None
        )
        _passkeys=(
            self.passkeys
            if hasattr(self, "passkeys")
            else None
        )
        _verify_event_subscription_enabled=(
            self.verify_event_subscription_enabled
            if hasattr(self, "verify_event_subscription_enabled")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _date_updated=(
            self.date_updated
            if hasattr(self, "date_updated")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _links=(
            self.links
            if hasattr(self, "links")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!r}, "
            f"account_sid={_account_sid!r}, "
            f"friendly_name={_friendly_name!r}, "
            f"code_length={_code_length!r}, "
            f"lookup_enabled={_lookup_enabled!r}, "
            f"psd_2_enabled={_psd_2_enabled!r}, "
            f"skip_sms_to_landlines={_skip_sms_to_landlines!r}, "
            f"dtmf_input_required={_dtmf_input_required!r}, "
            f"tts_name={_tts_name!r}, "
            f"do_not_share_warning_enabled={_do_not_share_warning_enabled!r}, "
            f"custom_code_enabled={_custom_code_enabled!r}, "
            f"push={_push!r}, "
            f"totp={_totp!r}, "
            f"default_template_sid={_default_template_sid!r}, "
            f"whatsapp={_whatsapp!r}, "
            f"passkeys={_passkeys!r}, "
            f"verify_event_subscription_enabled={_verify_event_subscription_enabled!r}, "
            f"date_created={_date_created!r}, "
            f"date_updated={_date_updated!r}, "
            f"url={_url!r}, "
            f"links={_links!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _friendly_name=(
            self.friendly_name
            if hasattr(self, "friendly_name")
            else None
        )
        _code_length=(
            self.code_length
            if hasattr(self, "code_length")
            else None
        )
        _lookup_enabled=(
            self.lookup_enabled
            if hasattr(self, "lookup_enabled")
            else None
        )
        _psd_2_enabled=(
            self.psd_2_enabled
            if hasattr(self, "psd_2_enabled")
            else None
        )
        _skip_sms_to_landlines=(
            self.skip_sms_to_landlines
            if hasattr(self, "skip_sms_to_landlines")
            else None
        )
        _dtmf_input_required=(
            self.dtmf_input_required
            if hasattr(self, "dtmf_input_required")
            else None
        )
        _tts_name=(
            self.tts_name
            if hasattr(self, "tts_name")
            else None
        )
        _do_not_share_warning_enabled=(
            self.do_not_share_warning_enabled
            if hasattr(self, "do_not_share_warning_enabled")
            else None
        )
        _custom_code_enabled=(
            self.custom_code_enabled
            if hasattr(self, "custom_code_enabled")
            else None
        )
        _push=(
            self.push
            if hasattr(self, "push")
            else None
        )
        _totp=(
            self.totp
            if hasattr(self, "totp")
            else None
        )
        _default_template_sid=(
            self.default_template_sid
            if hasattr(self, "default_template_sid")
            else None
        )
        _whatsapp=(
            self.whatsapp
            if hasattr(self, "whatsapp")
            else None
        )
        _passkeys=(
            self.passkeys
            if hasattr(self, "passkeys")
            else None
        )
        _verify_event_subscription_enabled=(
            self.verify_event_subscription_enabled
            if hasattr(self, "verify_event_subscription_enabled")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _date_updated=(
            self.date_updated
            if hasattr(self, "date_updated")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _links=(
            self.links
            if hasattr(self, "links")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!s}, "
            f"account_sid={_account_sid!s}, "
            f"friendly_name={_friendly_name!s}, "
            f"code_length={_code_length!s}, "
            f"lookup_enabled={_lookup_enabled!s}, "
            f"psd_2_enabled={_psd_2_enabled!s}, "
            f"skip_sms_to_landlines={_skip_sms_to_landlines!s}, "
            f"dtmf_input_required={_dtmf_input_required!s}, "
            f"tts_name={_tts_name!s}, "
            f"do_not_share_warning_enabled={_do_not_share_warning_enabled!s}, "
            f"custom_code_enabled={_custom_code_enabled!s}, "
            f"push={_push!s}, "
            f"totp={_totp!s}, "
            f"default_template_sid={_default_template_sid!s}, "
            f"whatsapp={_whatsapp!s}, "
            f"passkeys={_passkeys!s}, "
            f"verify_event_subscription_enabled={_verify_event_subscription_enabled!s}, "
            f"date_created={_date_created!s}, "
            f"date_updated={_date_updated!s}, "
            f"url={_url!s}, "
            f"links={_links!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
