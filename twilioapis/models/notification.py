"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper


class Notification(object):
    """Implementation of the 'Notification' model.

    Attributes:
        sid (str): The unique string that we created to identify the Notification
            resource.
        account_sid (str): The SID of the
            [Account](https://www.twilio.com/docs/iam/api/account) that created the
            Notification resource.
        service_sid (str): The SID of the
            [Service](https://www.twilio.com/docs/notify/api/service-resource) the
            resource is associated with.
        date_created (datetime): The date and time in GMT when the resource was
            created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        identities (List[str]): The list of `identity` values of the Users to notify.
            We will attempt to deliver notifications only to Bindings with an
            identity in this list.
        tags (List[str]): The tags that select the Bindings to notify. Notifications
            will be attempted only to Bindings that have all of the tags listed in
            this property.
        segments (List[str]): The list of Segments to notify. The
            [Segment](https://www.twilio.com/docs/notify/api/segment-resource)
            resource is deprecated. Use the `tags` property, instead.
        priority (NotifPriority): The priority of the notification. Can be: `low` or
            `high` and the default is `high`. A value of `low` optimizes the client
            app's battery consumption; however, notifications may be delivered with
            unspecified delay. For FCM and GCM, `low` priority is the same as
            `Normal` priority. For APNS `low` priority is the same as `5`. A value of
            `high` sends the notification immediately, and can wake up a sleeping
            device. For FCM and GCM, `high` is the same as `High` priority. For APNS,
            `high` is a priority `10`. SMS does not support this property.
        ttl (int): How long, in seconds, the notification is valid. Can be an integer
            between 0 and 2,419,200, which is 4 weeks, the default and the maximum
            supported time to live (TTL). Delivery should be attempted if the device
            is offline until the TTL elapses. Zero means that the notification
            delivery is attempted immediately, only once, and is not stored for
            future delivery. SMS does not support this property.
        title (str): The notification title. For FCM and GCM, this translates to the
            `data.twi_title` value. For APNS, this translates to the
            `aps.alert.title` value. SMS does not support this property. This field
            is not visible on iOS phones and tablets but appears on Apple Watch and
            Android devices.
        body (str): The notification text. For FCM and GCM, translates to
            `data.twi_body`. For APNS, translates to `aps.alert.body`. For SMS,
            translates to `body`. SMS requires either this `body` value, or
            `media_urls` attribute defined in the `sms` parameter of the notification.
        sound (str): The name of the sound to be played for the notification. For FCM
            and GCM, this Translates to `data.twi_sound`.  For APNS, this translates
            to `aps.sound`.  SMS does not support this property.
        action (str): The actions to display for the notification. For APNS,
            translates to the `aps.category` value. For GCM, translates to the
            `data.twi_action` value. For SMS, this parameter is not supported and is
            omitted from deliveries to those channels.
        data (Any): The custom key-value pairs of the notification's payload. For FCM
            and GCM, this value translates to `data` in the FCM and GCM payloads. FCM
            and GCM [reserve certain
            keys](https://firebase.google.com/docs/cloud-messaging/http-server-ref)
            that cannot be used in those channels. For APNS, attributes of `data` are
            inserted into the APNS payload as custom properties outside of the `aps`
            dictionary. In all channels, we reserve keys that start with `twi_` for
            future use. Custom keys that start with `twi_` are not allowed and are
            rejected as 400 Bad request with no delivery attempted. For SMS, this
            parameter is not supported and is omitted from deliveries to those
            channels.
        apn (Any): The APNS-specific payload that overrides corresponding attributes
            in the generic payload for APNS Bindings. This property maps to the APNS
            `Payload` item, therefore the `aps` key must be used to change standard
            attributes. Adds custom key-value pairs to the root of the dictionary.
            See the [APNS
            documentation](https://developer.apple.com/library/content/documentation/N
            etworkingInternet/Conceptual/RemoteNotificationsPG/CommunicatingwithAPNs.h
            tml) for more details. We reserve keys that start with `twi_` for future
            use. Custom keys that start with `twi_` are not allowed.
        gcm (Any): The GCM-specific payload that overrides corresponding attributes
            in the generic payload for GCM Bindings.  This property maps to the root
            JSON dictionary. Target parameters `to`, `registration_ids`, and
            `notification_key` are not allowed. We reserve keys that start with
            `twi_` for future use. Custom keys that start with `twi_` are not allowed.
        fcm (Any): The FCM-specific payload that overrides corresponding attributes
            in the generic payload for FCM Bindings. This property maps to the root
            JSON dictionary. See the [FCM
            documentation](https://firebase.google.com/docs/cloud-messaging/http-serve
            r-ref#downstream) for more details. Target parameters `to`,
            `registration_ids`, `condition`, and `notification_key` are not allowed
            in this parameter. We reserve keys that start with `twi_` for future use.
            Custom keys that start with `twi_` are not allowed. FCM also [reserves
            certain
            keys](https://firebase.google.com/docs/cloud-messaging/http-server-ref),
            which cannot be used in that channel.
        sms (Any): The SMS-specific payload that overrides corresponding attributes
            in the generic payload for SMS Bindings.  Each attribute in this value
            maps to the corresponding `form` parameter of the Twilio
            [Message](https://www.twilio.com/docs/sms/api/message-resource) resource.
            These parameters of the Message resource are supported in snake case
            format: `body`, `media_urls`, `status_callback`, and `max_price`.  The
            `status_callback` parameter overrides the corresponding parameter in the
            messaging service, if configured. The `media_urls` property expects a
            JSON array.
        facebook_messenger (Any): Deprecated.
        alexa (Any): Deprecated.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sid": "sid",
        "account_sid": "account_sid",
        "service_sid": "service_sid",
        "date_created": "date_created",
        "identities": "identities",
        "tags": "tags",
        "segments": "segments",
        "priority": "priority",
        "ttl": "ttl",
        "title": "title",
        "body": "body",
        "sound": "sound",
        "action": "action",
        "data": "data",
        "apn": "apn",
        "gcm": "gcm",
        "fcm": "fcm",
        "sms": "sms",
        "facebook_messenger": "facebook_messenger",
        "alexa": "alexa",
    }

    _optionals = [
        "sid",
        "account_sid",
        "service_sid",
        "date_created",
        "identities",
        "tags",
        "segments",
        "priority",
        "ttl",
        "title",
        "body",
        "sound",
        "action",
        "data",
        "apn",
        "gcm",
        "fcm",
        "sms",
        "facebook_messenger",
        "alexa",
    ]

    _nullables = [
        "sid",
        "account_sid",
        "service_sid",
        "date_created",
        "identities",
        "tags",
        "segments",
        "title",
        "body",
        "sound",
        "action",
        "data",
        "apn",
        "gcm",
        "fcm",
        "sms",
        "facebook_messenger",
        "alexa",
    ]

    def __init__(
        self,
        sid=APIHelper.SKIP,
        account_sid=APIHelper.SKIP,
        service_sid=APIHelper.SKIP,
        date_created=APIHelper.SKIP,
        identities=APIHelper.SKIP,
        tags=APIHelper.SKIP,
        segments=APIHelper.SKIP,
        priority=APIHelper.SKIP,
        ttl=0,
        title=APIHelper.SKIP,
        body=APIHelper.SKIP,
        sound=APIHelper.SKIP,
        action=APIHelper.SKIP,
        data=APIHelper.SKIP,
        apn=APIHelper.SKIP,
        gcm=APIHelper.SKIP,
        fcm=APIHelper.SKIP,
        sms=APIHelper.SKIP,
        facebook_messenger=APIHelper.SKIP,
        alexa=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Notification instance."""
        # Initialize members of the class
        if sid is not APIHelper.SKIP:
            self.sid = sid
        if account_sid is not APIHelper.SKIP:
            self.account_sid = account_sid
        if service_sid is not APIHelper.SKIP:
            self.service_sid = service_sid
        if date_created is not APIHelper.SKIP:
            self.date_created =\
                 APIHelper.apply_datetime_converter(
                date_created, APIHelper.RFC3339DateTime)\
                 if date_created else None
        if identities is not APIHelper.SKIP:
            self.identities = identities
        if tags is not APIHelper.SKIP:
            self.tags = tags
        if segments is not APIHelper.SKIP:
            self.segments = segments
        if priority is not APIHelper.SKIP:
            self.priority = priority
        self.ttl = ttl
        if title is not APIHelper.SKIP:
            self.title = title
        if body is not APIHelper.SKIP:
            self.body = body
        if sound is not APIHelper.SKIP:
            self.sound = sound
        if action is not APIHelper.SKIP:
            self.action = action
        if data is not APIHelper.SKIP:
            self.data = data
        if apn is not APIHelper.SKIP:
            self.apn = apn
        if gcm is not APIHelper.SKIP:
            self.gcm = gcm
        if fcm is not APIHelper.SKIP:
            self.fcm = fcm
        if sms is not APIHelper.SKIP:
            self.sms = sms
        if facebook_messenger is not APIHelper.SKIP:
            self.facebook_messenger = facebook_messenger
        if alexa is not APIHelper.SKIP:
            self.alexa = alexa

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sid =\
            dictionary.get("sid")\
            if "sid" in dictionary.keys()\
                else APIHelper.SKIP
        account_sid =\
            dictionary.get("account_sid")\
            if "account_sid" in dictionary.keys()\
                else APIHelper.SKIP
        service_sid =\
            dictionary.get("service_sid")\
            if "service_sid" in dictionary.keys()\
                else APIHelper.SKIP
        if "date_created" in dictionary.keys():
            date_created = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_created")).datetime\
                if dictionary.get("date_created") else None

        else:
            date_created = APIHelper.SKIP
        identities =\
            dictionary.get("identities")\
            if "identities" in dictionary.keys()\
                else APIHelper.SKIP
        tags =\
            dictionary.get("tags")\
            if "tags" in dictionary.keys()\
                else APIHelper.SKIP
        segments =\
            dictionary.get("segments")\
            if "segments" in dictionary.keys()\
                else APIHelper.SKIP
        priority =\
            dictionary.get("priority")\
            if dictionary.get("priority")\
                else APIHelper.SKIP
        ttl =\
            dictionary.get("ttl")\
            if dictionary.get("ttl")\
                else 0
        title =\
            dictionary.get("title")\
            if "title" in dictionary.keys()\
                else APIHelper.SKIP
        body =\
            dictionary.get("body")\
            if "body" in dictionary.keys()\
                else APIHelper.SKIP
        sound =\
            dictionary.get("sound")\
            if "sound" in dictionary.keys()\
                else APIHelper.SKIP
        action =\
            dictionary.get("action")\
            if "action" in dictionary.keys()\
                else APIHelper.SKIP
        data =\
            dictionary.get("data")\
            if "data" in dictionary.keys()\
                else APIHelper.SKIP
        apn =\
            dictionary.get("apn")\
            if "apn" in dictionary.keys()\
                else APIHelper.SKIP
        gcm =\
            dictionary.get("gcm")\
            if "gcm" in dictionary.keys()\
                else APIHelper.SKIP
        fcm =\
            dictionary.get("fcm")\
            if "fcm" in dictionary.keys()\
                else APIHelper.SKIP
        sms =\
            dictionary.get("sms")\
            if "sms" in dictionary.keys()\
                else APIHelper.SKIP
        facebook_messenger =\
            dictionary.get("facebook_messenger")\
            if "facebook_messenger" in dictionary.keys()\
                else APIHelper.SKIP
        alexa =\
            dictionary.get("alexa")\
            if "alexa" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(sid,
                   account_sid,
                   service_sid,
                   date_created,
                   identities,
                   tags,
                   segments,
                   priority,
                   ttl,
                   title,
                   body,
                   sound,
                   action,
                   data,
                   apn,
                   gcm,
                   fcm,
                   sms,
                   facebook_messenger,
                   alexa,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _service_sid=(
            self.service_sid
            if hasattr(self, "service_sid")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _identities=(
            self.identities
            if hasattr(self, "identities")
            else None
        )
        _tags=(
            self.tags
            if hasattr(self, "tags")
            else None
        )
        _segments=(
            self.segments
            if hasattr(self, "segments")
            else None
        )
        _priority=(
            self.priority
            if hasattr(self, "priority")
            else None
        )
        _ttl=(
            self.ttl
            if hasattr(self, "ttl")
            else None
        )
        _title=(
            self.title
            if hasattr(self, "title")
            else None
        )
        _body=(
            self.body
            if hasattr(self, "body")
            else None
        )
        _sound=(
            self.sound
            if hasattr(self, "sound")
            else None
        )
        _action=(
            self.action
            if hasattr(self, "action")
            else None
        )
        _data=(
            self.data
            if hasattr(self, "data")
            else None
        )
        _apn=(
            self.apn
            if hasattr(self, "apn")
            else None
        )
        _gcm=(
            self.gcm
            if hasattr(self, "gcm")
            else None
        )
        _fcm=(
            self.fcm
            if hasattr(self, "fcm")
            else None
        )
        _sms=(
            self.sms
            if hasattr(self, "sms")
            else None
        )
        _facebook_messenger=(
            self.facebook_messenger
            if hasattr(self, "facebook_messenger")
            else None
        )
        _alexa=(
            self.alexa
            if hasattr(self, "alexa")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!r}, "
            f"account_sid={_account_sid!r}, "
            f"service_sid={_service_sid!r}, "
            f"date_created={_date_created!r}, "
            f"identities={_identities!r}, "
            f"tags={_tags!r}, "
            f"segments={_segments!r}, "
            f"priority={_priority!r}, "
            f"ttl={_ttl!r}, "
            f"title={_title!r}, "
            f"body={_body!r}, "
            f"sound={_sound!r}, "
            f"action={_action!r}, "
            f"data={_data!r}, "
            f"apn={_apn!r}, "
            f"gcm={_gcm!r}, "
            f"fcm={_fcm!r}, "
            f"sms={_sms!r}, "
            f"facebook_messenger={_facebook_messenger!r}, "
            f"alexa={_alexa!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _service_sid=(
            self.service_sid
            if hasattr(self, "service_sid")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _identities=(
            self.identities
            if hasattr(self, "identities")
            else None
        )
        _tags=(
            self.tags
            if hasattr(self, "tags")
            else None
        )
        _segments=(
            self.segments
            if hasattr(self, "segments")
            else None
        )
        _priority=(
            self.priority
            if hasattr(self, "priority")
            else None
        )
        _ttl=(
            self.ttl
            if hasattr(self, "ttl")
            else None
        )
        _title=(
            self.title
            if hasattr(self, "title")
            else None
        )
        _body=(
            self.body
            if hasattr(self, "body")
            else None
        )
        _sound=(
            self.sound
            if hasattr(self, "sound")
            else None
        )
        _action=(
            self.action
            if hasattr(self, "action")
            else None
        )
        _data=(
            self.data
            if hasattr(self, "data")
            else None
        )
        _apn=(
            self.apn
            if hasattr(self, "apn")
            else None
        )
        _gcm=(
            self.gcm
            if hasattr(self, "gcm")
            else None
        )
        _fcm=(
            self.fcm
            if hasattr(self, "fcm")
            else None
        )
        _sms=(
            self.sms
            if hasattr(self, "sms")
            else None
        )
        _facebook_messenger=(
            self.facebook_messenger
            if hasattr(self, "facebook_messenger")
            else None
        )
        _alexa=(
            self.alexa
            if hasattr(self, "alexa")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!s}, "
            f"account_sid={_account_sid!s}, "
            f"service_sid={_service_sid!s}, "
            f"date_created={_date_created!s}, "
            f"identities={_identities!s}, "
            f"tags={_tags!s}, "
            f"segments={_segments!s}, "
            f"priority={_priority!s}, "
            f"ttl={_ttl!s}, "
            f"title={_title!s}, "
            f"body={_body!s}, "
            f"sound={_sound!s}, "
            f"action={_action!s}, "
            f"data={_data!s}, "
            f"apn={_apn!s}, "
            f"gcm={_gcm!s}, "
            f"fcm={_fcm!s}, "
            f"sms={_sms!s}, "
            f"facebook_messenger={_facebook_messenger!s}, "
            f"alexa={_alexa!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
