"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from twilioapis.api_helper import APIHelper


class Verification(object):
    """Implementation of the 'Verification' model.

    Attributes:
        sid (str): The unique string that we created to identify the Verification
            resource.
        service_sid (str): The SID of the
            [Service](https://www.twilio.com/docs/verify/api/service) the resource is
            associated with.
        account_sid (str): The SID of the
            [Account](https://www.twilio.com/docs/iam/api/account) that created the
            Verification resource.
        to (str): The phone number or
            [email](https://www.twilio.com/docs/verify/email) being verified. Phone
            numbers must be in [E.164
            format](https://www.twilio.com/docs/glossary/what-e164).
        channel (VerificationEnumChannel): The verification method used. One of:
            [`email`](https://www.twilio.com/docs/verify/email), `sms`, `whatsapp`,
            `call`, `sna`, or `rcs`.
        status (str): The status of the verification. Can be: `pending`, `approved`,
            `canceled`, `max_attempts_reached`, `deleted`, `failed` or `expired`.
        valid (bool): Use "status" instead. Legacy property indicating whether the
            verification was successful.
        lookup (Any): Information about the phone number being verified.
        amount (str): The amount of the associated PSD2 compliant transaction.
            Requires the PSD2 Service flag enabled.
        payee (str): The payee of the associated PSD2 compliant transaction. Requires
            the PSD2 Service flag enabled.
        send_code_attempts (List[Any]): An array of verification attempt objects
            containing the channel attempted and the channel-specific transaction SID.
        date_created (datetime): The date and time in GMT when the resource was
            created specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        date_updated (datetime): The date and time in GMT when the resource was last
            updated specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt)
            format.
        sna (Any): The set of fields used for a silent network auth (`sna`)
            verification. Contains a single field with the URL to be invoked to
            verify the phone number.
        url (str): The absolute URL of the Verification resource.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sid": "sid",
        "service_sid": "service_sid",
        "account_sid": "account_sid",
        "to": "to",
        "channel": "channel",
        "status": "status",
        "valid": "valid",
        "lookup": "lookup",
        "amount": "amount",
        "payee": "payee",
        "send_code_attempts": "send_code_attempts",
        "date_created": "date_created",
        "date_updated": "date_updated",
        "sna": "sna",
        "url": "url",
    }

    _optionals = [
        "sid",
        "service_sid",
        "account_sid",
        "to",
        "channel",
        "status",
        "valid",
        "lookup",
        "amount",
        "payee",
        "send_code_attempts",
        "date_created",
        "date_updated",
        "sna",
        "url",
    ]

    _nullables = [
        "sid",
        "service_sid",
        "account_sid",
        "to",
        "status",
        "valid",
        "lookup",
        "amount",
        "payee",
        "send_code_attempts",
        "date_created",
        "date_updated",
        "sna",
        "url",
    ]

    def __init__(
        self,
        sid=APIHelper.SKIP,
        service_sid=APIHelper.SKIP,
        account_sid=APIHelper.SKIP,
        to=APIHelper.SKIP,
        channel=APIHelper.SKIP,
        status=APIHelper.SKIP,
        valid=APIHelper.SKIP,
        lookup=APIHelper.SKIP,
        amount=APIHelper.SKIP,
        payee=APIHelper.SKIP,
        send_code_attempts=APIHelper.SKIP,
        date_created=APIHelper.SKIP,
        date_updated=APIHelper.SKIP,
        sna=APIHelper.SKIP,
        url=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Verification instance."""
        # Initialize members of the class
        if sid is not APIHelper.SKIP:
            self.sid = sid
        if service_sid is not APIHelper.SKIP:
            self.service_sid = service_sid
        if account_sid is not APIHelper.SKIP:
            self.account_sid = account_sid
        if to is not APIHelper.SKIP:
            self.to = to
        if channel is not APIHelper.SKIP:
            self.channel = channel
        if status is not APIHelper.SKIP:
            self.status = status
        if valid is not APIHelper.SKIP:
            self.valid = valid
        if lookup is not APIHelper.SKIP:
            self.lookup = lookup
        if amount is not APIHelper.SKIP:
            self.amount = amount
        if payee is not APIHelper.SKIP:
            self.payee = payee
        if send_code_attempts is not APIHelper.SKIP:
            self.send_code_attempts = send_code_attempts
        if date_created is not APIHelper.SKIP:
            self.date_created =\
                 APIHelper.apply_datetime_converter(
                date_created, APIHelper.RFC3339DateTime)\
                 if date_created else None
        if date_updated is not APIHelper.SKIP:
            self.date_updated =\
                 APIHelper.apply_datetime_converter(
                date_updated, APIHelper.RFC3339DateTime)\
                 if date_updated else None
        if sna is not APIHelper.SKIP:
            self.sna = sna
        if url is not APIHelper.SKIP:
            self.url = url

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sid =\
            dictionary.get("sid")\
            if "sid" in dictionary.keys()\
                else APIHelper.SKIP
        service_sid =\
            dictionary.get("service_sid")\
            if "service_sid" in dictionary.keys()\
                else APIHelper.SKIP
        account_sid =\
            dictionary.get("account_sid")\
            if "account_sid" in dictionary.keys()\
                else APIHelper.SKIP
        to =\
            dictionary.get("to")\
            if "to" in dictionary.keys()\
                else APIHelper.SKIP
        channel =\
            dictionary.get("channel")\
            if dictionary.get("channel")\
                else APIHelper.SKIP
        status =\
            dictionary.get("status")\
            if "status" in dictionary.keys()\
                else APIHelper.SKIP
        valid =\
            dictionary.get("valid")\
            if "valid" in dictionary.keys()\
                else APIHelper.SKIP
        lookup =\
            dictionary.get("lookup")\
            if "lookup" in dictionary.keys()\
                else APIHelper.SKIP
        amount =\
            dictionary.get("amount")\
            if "amount" in dictionary.keys()\
                else APIHelper.SKIP
        payee =\
            dictionary.get("payee")\
            if "payee" in dictionary.keys()\
                else APIHelper.SKIP
        send_code_attempts =\
            dictionary.get("send_code_attempts")\
            if "send_code_attempts" in dictionary.keys()\
                else APIHelper.SKIP
        if "date_created" in dictionary.keys():
            date_created = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_created")).datetime\
                if dictionary.get("date_created") else None

        else:
            date_created = APIHelper.SKIP
        if "date_updated" in dictionary.keys():
            date_updated = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("date_updated")).datetime\
                if dictionary.get("date_updated") else None

        else:
            date_updated = APIHelper.SKIP
        sna =\
            dictionary.get("sna")\
            if "sna" in dictionary.keys()\
                else APIHelper.SKIP
        url =\
            dictionary.get("url")\
            if "url" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(sid,
                   service_sid,
                   account_sid,
                   to,
                   channel,
                   status,
                   valid,
                   lookup,
                   amount,
                   payee,
                   send_code_attempts,
                   date_created,
                   date_updated,
                   sna,
                   url,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _service_sid=(
            self.service_sid
            if hasattr(self, "service_sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _to=(
            self.to
            if hasattr(self, "to")
            else None
        )
        _channel=(
            self.channel
            if hasattr(self, "channel")
            else None
        )
        _status=(
            self.status
            if hasattr(self, "status")
            else None
        )
        _valid=(
            self.valid
            if hasattr(self, "valid")
            else None
        )
        _lookup=(
            self.lookup
            if hasattr(self, "lookup")
            else None
        )
        _amount=(
            self.amount
            if hasattr(self, "amount")
            else None
        )
        _payee=(
            self.payee
            if hasattr(self, "payee")
            else None
        )
        _send_code_attempts=(
            self.send_code_attempts
            if hasattr(self, "send_code_attempts")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _date_updated=(
            self.date_updated
            if hasattr(self, "date_updated")
            else None
        )
        _sna=(
            self.sna
            if hasattr(self, "sna")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!r}, "
            f"service_sid={_service_sid!r}, "
            f"account_sid={_account_sid!r}, "
            f"to={_to!r}, "
            f"channel={_channel!r}, "
            f"status={_status!r}, "
            f"valid={_valid!r}, "
            f"lookup={_lookup!r}, "
            f"amount={_amount!r}, "
            f"payee={_payee!r}, "
            f"send_code_attempts={_send_code_attempts!r}, "
            f"date_created={_date_created!r}, "
            f"date_updated={_date_updated!r}, "
            f"sna={_sna!r}, "
            f"url={_url!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _sid=(
            self.sid
            if hasattr(self, "sid")
            else None
        )
        _service_sid=(
            self.service_sid
            if hasattr(self, "service_sid")
            else None
        )
        _account_sid=(
            self.account_sid
            if hasattr(self, "account_sid")
            else None
        )
        _to=(
            self.to
            if hasattr(self, "to")
            else None
        )
        _channel=(
            self.channel
            if hasattr(self, "channel")
            else None
        )
        _status=(
            self.status
            if hasattr(self, "status")
            else None
        )
        _valid=(
            self.valid
            if hasattr(self, "valid")
            else None
        )
        _lookup=(
            self.lookup
            if hasattr(self, "lookup")
            else None
        )
        _amount=(
            self.amount
            if hasattr(self, "amount")
            else None
        )
        _payee=(
            self.payee
            if hasattr(self, "payee")
            else None
        )
        _send_code_attempts=(
            self.send_code_attempts
            if hasattr(self, "send_code_attempts")
            else None
        )
        _date_created=(
            self.date_created
            if hasattr(self, "date_created")
            else None
        )
        _date_updated=(
            self.date_updated
            if hasattr(self, "date_updated")
            else None
        )
        _sna=(
            self.sna
            if hasattr(self, "sna")
            else None
        )
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"sid={_sid!s}, "
            f"service_sid={_service_sid!s}, "
            f"account_sid={_account_sid!s}, "
            f"to={_to!s}, "
            f"channel={_channel!s}, "
            f"status={_status!s}, "
            f"valid={_valid!s}, "
            f"lookup={_lookup!s}, "
            f"amount={_amount!s}, "
            f"payee={_payee!s}, "
            f"send_code_attempts={_send_code_attempts!s}, "
            f"date_created={_date_created!s}, "
            f"date_updated={_date_updated!s}, "
            f"sna={_sna!s}, "
            f"url={_url!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
