"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from twilioapis.api_helper import APIHelper
from twilioapis.apis.base_api import BaseApi
from twilioapis.configuration import Server
from twilioapis.http.http_method_enum import (
    HttpMethodEnum,
)
from twilioapis.models.conversation_participant import (
    ConversationParticipant,
)
from twilioapis.models.list_conversation_participant_response import (
    ListConversationParticipantResponse,
)
from twilioapis.models.list_service_conversation_participant_response import (
    ListServiceConversationParticipantResponse,
)
from twilioapis.models.service_conversation_participant import (
    ServiceConversationParticipant,
)


class ConversationsV1ParticipantApi(BaseApi):
    """A Controller to access Endpoints in the twilioapis API."""

    def __init__(self, config):
        """Initialize ConversationsV1ParticipantApi object."""
        super(ConversationsV1ParticipantApi, self).__init__(config)

    def create_conversation_participant(self,
                                        conversation_sid,
                                        x_twilio_webhook_enabled=None,
                                        identity=None,
                                        messaging_binding_address=None,
                                        messaging_binding_proxy_address=None,
                                        date_created=None,
                                        date_updated=None,
                                        attributes=None,
                                        messaging_binding_projected_address=None,
                                        role_sid=None):
        """Perform a POST request to
        /v1/Conversations/{ConversationSid}/Participants.

        Add a new participant to the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            identity (str, optional): A unique string identifier for the conversation
                participant as [Conversation
                User](https://www.twilio.com/docs/conversations/api/user-resource).
                This parameter is non-null if (and only if) the participant is using
                the Conversations SDK to communicate. Limited to 256 characters.
            messaging_binding_address (str, optional): The address of the
                participant's device, e.g. a phone or WhatsApp number. Together with
                the Proxy address, this determines a participant uniquely. This field
                (with proxy_address) is only null when the participant is interacting
                from an SDK endpoint (see the 'identity' field).
            messaging_binding_proxy_address (str, optional): The address of the
                Twilio phone number (or WhatsApp number) that the participant is in
                contact with. This field, together with participant address, is only
                null when the participant is interacting from an SDK endpoint (see
                the 'identity' field).
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set "{}" will be returned.
            messaging_binding_projected_address (str, optional): The address of the
                Twilio phone number that is used in Group MMS. Communication mask for
                the Conversation participant with Identity.
            role_sid (str, optional): The SID of a conversation-level
                [Role](https://www.twilio.com/docs/conversations/api/role-resource)
                to assign to the participant.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Participants")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("Identity")
                .value(identity))
            .form_param(Parameter()
                .key("MessagingBinding.Address")
                .value(messaging_binding_address))
            .form_param(Parameter()
                .key("MessagingBinding.ProxyAddress")
                .value(messaging_binding_proxy_address))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MessagingBinding.ProjectedAddress")
                .value(messaging_binding_projected_address))
            .form_param(Parameter()
                .key("RoleSid")
                .value(role_sid))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationParticipant.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_conversation_participant(self,
                                      conversation_sid,
                                      page_size=None,
                                      page=None,
                                      page_token=None):
        """Perform a GET request to
        /v1/Conversations/{ConversationSid}/Participants.

        Retrieve a list of all participants of the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for participants.
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 100.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Participants")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListConversationParticipantResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_conversation_participant(self,
                                        conversation_sid,
                                        sid,
                                        x_twilio_webhook_enabled=None,
                                        date_created=None,
                                        date_updated=None,
                                        attributes=None,
                                        role_sid=None,
                                        messaging_binding_proxy_address=None,
                                        messaging_binding_projected_address=None,
                                        identity=None,
                                        last_read_message_index=None,
                                        last_read_timestamp=None):
        """Perform a POST request to
        /v1/Conversations/{ConversationSid}/Participants/{Sid}.

        Update an existing participant in the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set "{}" will be returned.
            role_sid (str, optional): The SID of a conversation-level
                [Role](https://www.twilio.com/docs/conversations/api/role-resource)
                to assign to the participant.
            messaging_binding_proxy_address (str, optional): The address of the
                Twilio phone number that the participant is in contact with. 'null'
                value will remove it.
            messaging_binding_projected_address (str, optional): The address of the
                Twilio phone number that is used in Group MMS. 'null' value will
                remove it.
            identity (str, optional): A unique string identifier for the conversation
                participant as [Conversation
                User](https://www.twilio.com/docs/conversations/api/user-resource).
                This parameter is non-null if (and only if) the participant is using
                the Conversations SDK to communicate. Limited to 256 characters.
            last_read_message_index (int, optional): Index of last “read” message in
                the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for the Participant.
            last_read_timestamp (str, optional): Timestamp of last “read” message in
                the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for the Participant.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Participants/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("RoleSid")
                .value(role_sid))
            .form_param(Parameter()
                .key("MessagingBinding.ProxyAddress")
                .value(messaging_binding_proxy_address))
            .form_param(Parameter()
                .key("MessagingBinding.ProjectedAddress")
                .value(messaging_binding_projected_address))
            .form_param(Parameter()
                .key("Identity")
                .value(identity))
            .form_param(Parameter()
                .key("LastReadMessageIndex")
                .value(last_read_message_index))
            .form_param(Parameter()
                .key("LastReadTimestamp")
                .value(last_read_timestamp))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationParticipant.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_conversation_participant(self,
                                        conversation_sid,
                                        sid,
                                        x_twilio_webhook_enabled=None):
        """Perform a DELETE request to
        /v1/Conversations/{ConversationSid}/Participants/{Sid}.

        Remove a participant from the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Participants/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_conversation_participant(self,
                                       conversation_sid,
                                       sid):
        """Perform a GET request to
        /v1/Conversations/{ConversationSid}/Participants/{Sid}.

        Fetch a participant of the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            sid (str): A 34 character string that uniquely identifies this resource.
                Alternatively, you can pass a Participant's `identity` rather than
                the SID.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Participants/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationParticipant.from_dictionary)
            .is_api_response(True),
        ).execute()

    def create_service_conversation_participant(self,
                                                chat_service_sid,
                                                conversation_sid,
                                                x_twilio_webhook_enabled=None,
                                                identity=None,
                                                messaging_binding_address=None,
                                                messaging_binding_proxy_address=None,
                                                date_created=None,
                                                date_updated=None,
                                                attributes=None,
                                                messaging_binding_projected_address=None,
                                                role_sid=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants.

        Add a new participant to the conversation in a specific service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            identity (str, optional): A unique string identifier for the conversation
                participant as [Conversation
                User](https://www.twilio.com/docs/conversations/api/user-resource).
                This parameter is non-null if (and only if) the participant is using
                the [Conversation
                SDK](https://www.twilio.com/docs/conversations/sdk-overview) to
                communicate. Limited to 256 characters.
            messaging_binding_address (str, optional): The address of the
                participant's device, e.g. a phone or WhatsApp number. Together with
                the Proxy address, this determines a participant uniquely. This field
                (with `proxy_address`) is only null when the participant is
                interacting from an SDK endpoint (see the `identity` field).
            messaging_binding_proxy_address (str, optional): The address of the
                Twilio phone number (or WhatsApp number) that the participant is in
                contact with. This field, together with participant address, is only
                null when the participant is interacting from an SDK endpoint (see
                the `identity` field).
            date_created (datetime, optional): The date on which this resource was
                created.
            date_updated (datetime, optional): The date on which this resource was
                last updated.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set `{}` will be returned.
            messaging_binding_projected_address (str, optional): The address of the
                Twilio phone number that is used in Group MMS.
            role_sid (str, optional): The SID of a conversation-level
                [Role](https://www.twilio.com/docs/conversations/api/role-resource)
                to assign to the participant.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("Identity")
                .value(identity))
            .form_param(Parameter()
                .key("MessagingBinding.Address")
                .value(messaging_binding_address))
            .form_param(Parameter()
                .key("MessagingBinding.ProxyAddress")
                .value(messaging_binding_proxy_address))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MessagingBinding.ProjectedAddress")
                .value(messaging_binding_projected_address))
            .form_param(Parameter()
                .key("RoleSid")
                .value(role_sid))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationParticipant.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_service_conversation_participant(self,
                                              chat_service_sid,
                                              conversation_sid,
                                              page_size=None,
                                              page=None,
                                              page_token=None):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants.

        Retrieve a list of all participants of the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for participants.
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 100.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListServiceConversationParticipantResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_service_conversation_participant(self,
                                                chat_service_sid,
                                                conversation_sid,
                                                sid,
                                                x_twilio_webhook_enabled=None,
                                                date_created=None,
                                                date_updated=None,
                                                identity=None,
                                                attributes=None,
                                                role_sid=None,
                                                messaging_binding_proxy_address=None,
                                                messaging_binding_projected_address=None,
                                                last_read_message_index=None,
                                                last_read_timestamp=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid
        }.

        Update an existing participant in the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            date_created (datetime, optional): The date on which this resource was
                created.
            date_updated (datetime, optional): The date on which this resource was
                last updated.
            identity (str, optional): A unique string identifier for the conversation
                participant as [Conversation
                User](https://www.twilio.com/docs/conversations/api/user-resource).
                This parameter is non-null if (and only if) the participant is using
                the [Conversation
                SDK](https://www.twilio.com/docs/conversations/sdk-overview) to
                communicate. Limited to 256 characters.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set `{}` will be returned.
            role_sid (str, optional): The SID of a conversation-level
                [Role](https://www.twilio.com/docs/conversations/api/role-resource)
                to assign to the participant.
            messaging_binding_proxy_address (str, optional): The address of the
                Twilio phone number that the participant is in contact with. 'null'
                value will remove it.
            messaging_binding_projected_address (str, optional): The address of the
                Twilio phone number that is used in Group MMS. 'null' value will
                remove it.
            last_read_message_index (int, optional): Index of last “read” message in
                the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for the Participant.
            last_read_timestamp (str, optional): Timestamp of last “read” message in
                the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for the Participant.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Identity")
                .value(identity))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("RoleSid")
                .value(role_sid))
            .form_param(Parameter()
                .key("MessagingBinding.ProxyAddress")
                .value(messaging_binding_proxy_address))
            .form_param(Parameter()
                .key("MessagingBinding.ProjectedAddress")
                .value(messaging_binding_projected_address))
            .form_param(Parameter()
                .key("LastReadMessageIndex")
                .value(last_read_message_index))
            .form_param(Parameter()
                .key("LastReadTimestamp")
                .value(last_read_timestamp))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationParticipant.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_service_conversation_participant(self,
                                                chat_service_sid,
                                                conversation_sid,
                                                sid,
                                                x_twilio_webhook_enabled=None):
        """Perform a DELETE request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid
        }.

        Remove a participant from the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_service_conversation_participant(self,
                                               chat_service_sid,
                                               conversation_sid,
                                               sid):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid
        }.

        Fetch a participant of the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this participant.
            sid (str): A 34 character string that uniquely identifies this resource.
                Alternatively, you can pass a Participant's `identity` rather than
                the SID.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Participants/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationParticipant.from_dictionary)
            .is_api_response(True),
        ).execute()
