"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from twilioapis.api_helper import APIHelper
from twilioapis.apis.base_api import BaseApi
from twilioapis.configuration import Server
from twilioapis.http.http_method_enum import (
    HttpMethodEnum,
)
from twilioapis.models.service_2 import Service2


class VerifyV2ServiceApi(BaseApi):
    """A Controller to access Endpoints in the twilioapis API."""

    def __init__(self, config):
        """Initialize VerifyV2ServiceApi object."""
        super(VerifyV2ServiceApi, self).__init__(config)

    def create_service(self,
                       friendly_name,
                       code_length=None,
                       lookup_enabled=None,
                       skip_sms_to_landlines=None,
                       dtmf_input_required=None,
                       tts_name=None,
                       psd_2_enabled=None,
                       do_not_share_warning_enabled=None,
                       custom_code_enabled=None,
                       push_include_date=None,
                       push_apn_credential_sid=None,
                       push_fcm_credential_sid=None,
                       totp_issuer=None,
                       totp_time_step=None,
                       totp_code_length=None,
                       totp_skew=None,
                       default_template_sid=None,
                       whatsapp_msg_service_sid=None,
                       whatsapp_from=None,
                       passkeys_relying_party_id=None,
                       passkeys_relying_party_name=None,
                       passkeys_relying_party_origins=None,
                       passkeys_authenticator_attachment=None,
                       passkeys_discoverable_credentials=None,
                       passkeys_user_verification=None,
                       verify_event_subscription_enabled=None):
        """Perform a POST request to /v2/Services.

        Create a new Verification Service.

        Args:
            friendly_name (str): A descriptive string that you create to describe the
                verification service. It can be up to 32 characters long. **This
                value should not contain PII.**
            code_length (int, optional): The length of the verification code to
                generate. Must be an integer value between 4 and 10, inclusive.
            lookup_enabled (bool, optional): Whether to perform a lookup with each
                verification started and return info about the phone number.
            skip_sms_to_landlines (bool, optional): Whether to skip sending SMS
                verifications to landlines. Requires `lookup_enabled`.
            dtmf_input_required (bool, optional): Whether to ask the user to press a
                number before delivering the verify code in a phone call.
            tts_name (str, optional): The name of an alternative text-to-speech
                service to use in phone calls. Applies only to TTS languages.
            psd_2_enabled (bool, optional): Whether to pass PSD2 transaction
                parameters when starting a verification.
            do_not_share_warning_enabled (bool, optional): Whether to add a security
                warning at the end of an SMS verification body. Disabled by default
                and applies only to SMS. Example SMS body: `Your AppName verification
                code is: 1234. Don’t share this code with anyone; our employees will
                never ask for the code`
            custom_code_enabled (bool, optional): Whether to allow sending
                verifications with a custom code instead of a randomly generated one.
            push_include_date (bool, optional): Optional configuration for the Push
                factors. If true, include the date in the Challenge's response.
                Otherwise, the date is omitted from the response. See
                [Challenge](https://www.twilio.com/docs/verify/api/challenge)
                resource’s details parameter for more info. Default: false.
                **Deprecated** do not use this parameter. This timestamp value is the
                same one as the one found in `date_created`, please use that one
                instead.
            push_apn_credential_sid (str, optional): Optional configuration for the
                Push factors. Set the APN Credential for this service. This will
                allow to send push notifications to iOS devices. See [Credential
                Resource](https://www.twilio.com/docs/notify/api/credential-resource)
            push_fcm_credential_sid (str, optional): Optional configuration for the
                Push factors. Set the FCM Credential for this service. This will
                allow to send push notifications to Android devices. See [Credential
                Resource](https://www.twilio.com/docs/notify/api/credential-resource)
            totp_issuer (str, optional): Optional configuration for the TOTP factors.
                Set TOTP Issuer for this service. This will allow to configure the
                issuer of the TOTP URI. Defaults to the service friendly name if not
                provided.
            totp_time_step (int, optional): Optional configuration for the TOTP
                factors. Defines how often, in seconds, are TOTP codes generated.
                i.e, a new TOTP code is generated every time_step seconds. Must be
                between 20 and 60 seconds, inclusive. Defaults to 30 seconds
            totp_code_length (int, optional): Optional configuration for the TOTP
                factors. Number of digits for generated TOTP codes. Must be between 3
                and 8, inclusive. Defaults to 6
            totp_skew (int, optional): Optional configuration for the TOTP factors.
                The number of time-steps, past and future, that are valid for
                validation of TOTP codes. Must be between 0 and 2, inclusive.
                Defaults to 1
            default_template_sid (str, optional): The default message
                [template](https://www.twilio.com/docs/verify/api/templates). Will be
                used for all SMS verifications unless explicitly overriden. SMS
                channel only.
            whatsapp_msg_service_sid (str, optional): The SID of the Messaging
                Service containing WhatsApp Sender(s) that Verify will use to send
                WhatsApp messages to your users.
            whatsapp_from (str, optional): The number to use as the WhatsApp Sender
                that Verify will use to send WhatsApp messages to your users.This
                WhatsApp Sender must be associated with a Messaging Service SID.
            passkeys_relying_party_id (str, optional): The Relying Party ID for
                Passkeys. This is the domain of your application, e.g. `example.com`.
                It is used to identify your application when creating Passkeys.
            passkeys_relying_party_name (str, optional): The Relying Party Name for
                Passkeys. This is the name of your application, e.g. `Example App`.
                It is used to identify your application when creating Passkeys.
            passkeys_relying_party_origins (str, optional): The Relying Party Origins
                for Passkeys. This is the origin of your application, e.g.
                `login.example.com,www.example.com`. It is used to identify your
                application when creating Passkeys, it can have multiple origins
                split by `,`.
            passkeys_authenticator_attachment (str, optional): The Authenticator
                Attachment for Passkeys. This is the type of authenticator that will
                be used to create Passkeys. It can be empty or it can have the values
                `platform`, `cross-platform` or `any`.
            passkeys_discoverable_credentials (str, optional): Indicates whether
                credentials must be discoverable by the authenticator. It can be
                empty or it can have the values `required`, `preferred` or
                `discouraged`.
            passkeys_user_verification (str, optional): The User Verification for
                Passkeys. This is the type of user verification that will be used to
                create Passkeys. It can be empty or it can have the values
                `required`, `preferred` or `discouraged`.
            verify_event_subscription_enabled (bool, optional): Whether to allow
                verifications from the service to reach the stream-events sinks if
                configured

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT5)
            .path("/v2/Services")
            .http_method(HttpMethodEnum.POST)
            .form_param(Parameter()
                .key("FriendlyName")
                .value(friendly_name)
                .is_required(True))
            .form_param(Parameter()
                .key("CodeLength")
                .value(code_length))
            .form_param(Parameter()
                .key("LookupEnabled")
                .value(lookup_enabled))
            .form_param(Parameter()
                .key("SkipSmsToLandlines")
                .value(skip_sms_to_landlines))
            .form_param(Parameter()
                .key("DtmfInputRequired")
                .value(dtmf_input_required))
            .form_param(Parameter()
                .key("TtsName")
                .value(tts_name))
            .form_param(Parameter()
                .key("Psd2Enabled")
                .value(psd_2_enabled))
            .form_param(Parameter()
                .key("DoNotShareWarningEnabled")
                .value(do_not_share_warning_enabled))
            .form_param(Parameter()
                .key("CustomCodeEnabled")
                .value(custom_code_enabled))
            .form_param(Parameter()
                .key("Push.IncludeDate")
                .value(push_include_date))
            .form_param(Parameter()
                .key("Push.ApnCredentialSid")
                .value(push_apn_credential_sid))
            .form_param(Parameter()
                .key("Push.FcmCredentialSid")
                .value(push_fcm_credential_sid))
            .form_param(Parameter()
                .key("Totp.Issuer")
                .value(totp_issuer))
            .form_param(Parameter()
                .key("Totp.TimeStep")
                .value(totp_time_step))
            .form_param(Parameter()
                .key("Totp.CodeLength")
                .value(totp_code_length))
            .form_param(Parameter()
                .key("Totp.Skew")
                .value(totp_skew))
            .form_param(Parameter()
                .key("DefaultTemplateSid")
                .value(default_template_sid))
            .form_param(Parameter()
                .key("Whatsapp.MsgServiceSid")
                .value(whatsapp_msg_service_sid))
            .form_param(Parameter()
                .key("Whatsapp.From")
                .value(whatsapp_from))
            .form_param(Parameter()
                .key("Passkeys.RelyingParty.Id")
                .value(passkeys_relying_party_id))
            .form_param(Parameter()
                .key("Passkeys.RelyingParty.Name")
                .value(passkeys_relying_party_name))
            .form_param(Parameter()
                .key("Passkeys.RelyingParty.Origins")
                .value(passkeys_relying_party_origins))
            .form_param(Parameter()
                .key("Passkeys.AuthenticatorAttachment")
                .value(passkeys_authenticator_attachment))
            .form_param(Parameter()
                .key("Passkeys.DiscoverableCredentials")
                .value(passkeys_discoverable_credentials))
            .form_param(Parameter()
                .key("Passkeys.UserVerification")
                .value(passkeys_user_verification))
            .form_param(Parameter()
                .key("VerifyEventSubscriptionEnabled")
                .value(verify_event_subscription_enabled))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Service2.from_dictionary)
            .is_api_response(True),
        ).execute()
