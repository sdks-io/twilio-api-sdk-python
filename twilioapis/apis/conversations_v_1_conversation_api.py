"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from twilioapis.api_helper import APIHelper
from twilioapis.apis.base_api import BaseApi
from twilioapis.configuration import Server
from twilioapis.http.http_method_enum import (
    HttpMethodEnum,
)
from twilioapis.models.conversation import Conversation
from twilioapis.models.list_conversation_response import (
    ListConversationResponse,
)
from twilioapis.models.list_service_conversation_response import (
    ListServiceConversationResponse,
)
from twilioapis.models.service_conversation import (
    ServiceConversation,
)


class ConversationsV1ConversationApi(BaseApi):
    """A Controller to access Endpoints in the twilioapis API."""

    def __init__(self, config):
        """Initialize ConversationsV1ConversationApi object."""
        super(ConversationsV1ConversationApi, self).__init__(config)

    def create_conversation(self,
                            x_twilio_webhook_enabled=None,
                            friendly_name=None,
                            unique_name=None,
                            date_created=None,
                            date_updated=None,
                            messaging_service_sid=None,
                            attributes=None,
                            state=None,
                            timers_inactive=None,
                            timers_closed=None,
                            bindings_email_address=None,
                            bindings_email_name=None):
        """Perform a POST request to /v1/Conversations.

        Create a new conversation in your account's default service

        Args:
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            friendly_name (str, optional): The human-readable name of this
                conversation, limited to 256 characters. Optional.
            unique_name (str, optional): An application-defined string that uniquely
                identifies the resource. It can be used to address the resource in
                place of the resource's `sid` in the URL.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated.
            messaging_service_sid (str, optional): The unique ID of the [Messaging
                Service](https://www.twilio.com/docs/messaging/api/service-resource)
                this conversation belongs to.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set "{}" will be returned.
            state (ConversationState, optional): Current state of this conversation.
                Can be either `initializing`, `active`, `inactive` or `closed` and
                defaults to `active`
            timers_inactive (str, optional): ISO8601 duration when conversation will
                be switched to `inactive` state. Minimum value for this timer is 1
                minute.
            timers_closed (str, optional): ISO8601 duration when conversation will be
                switched to `closed` state. Minimum value for this timer is 10
                minutes.
            bindings_email_address (str, optional): The default email address that
                will be used when sending outbound emails in this conversation.
            bindings_email_name (str, optional): The default name that will be used
                when sending outbound emails in this conversation.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("FriendlyName")
                .value(friendly_name))
            .form_param(Parameter()
                .key("UniqueName")
                .value(unique_name))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("MessagingServiceSid")
                .value(messaging_service_sid))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("State")
                .value(state))
            .form_param(Parameter()
                .key("Timers.Inactive")
                .value(timers_inactive))
            .form_param(Parameter()
                .key("Timers.Closed")
                .value(timers_closed))
            .form_param(Parameter()
                .key("Bindings.Email.Address")
                .value(bindings_email_address))
            .form_param(Parameter()
                .key("Bindings.Email.Name")
                .value(bindings_email_name))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Conversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_conversation(self,
                          start_date=None,
                          end_date=None,
                          state=None,
                          page_size=None,
                          page=None,
                          page_token=None):
        """Perform a GET request to /v1/Conversations.

        Retrieve a list of conversations in your account's default service

        Args:
            start_date (str, optional): Specifies the beginning of the date range for
                filtering Conversations based on their creation date. Conversations
                that were created on or after this date will be included in the
                results. The date must be in ISO8601 format, specifically starting at
                the beginning of the specified date (YYYY-MM-DDT00:00:00Z), for
                precise filtering. This parameter can be combined with other filters.
                If this filter is used, the returned list is sorted by latest
                conversation creation date in descending order.
            end_date (str, optional): Defines the end of the date range for filtering
                conversations by their creation date. Only conversations that were
                created on or before this date will appear in the results.  The date
                must be in ISO8601 format, specifically capturing up to the end of
                the specified date (YYYY-MM-DDT23:59:59Z), to ensure that
                conversations from the entire end day are included. This parameter
                can be combined with other filters. If this filter is used, the
                returned list is sorted by latest conversation creation date in
                descending order.
            state (ConversationState, optional): State for sorting and filtering list
                of Conversations. Can be `active`, `inactive` or `closed`
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 100.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations")
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                .key("StartDate")
                .value(start_date))
            .query_param(Parameter()
                .key("EndDate")
                .value(end_date))
            .query_param(Parameter()
                .key("State")
                .value(state))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListConversationResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_conversation(self,
                            sid,
                            x_twilio_webhook_enabled=None,
                            friendly_name=None,
                            date_created=None,
                            date_updated=None,
                            attributes=None,
                            messaging_service_sid=None,
                            state=None,
                            timers_inactive=None,
                            timers_closed=None,
                            unique_name=None,
                            bindings_email_address=None,
                            bindings_email_name=None):
        """Perform a POST request to /v1/Conversations/{Sid}.

        Update an existing conversation in your account's default service

        Args:
            sid (str): A 34 character string that uniquely identifies this resource.
                Can also be the `unique_name` of the Conversation.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            friendly_name (str, optional): The human-readable name of this
                conversation, limited to 256 characters. Optional.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set "{}" will be returned.
            messaging_service_sid (str, optional): The unique ID of the [Messaging
                Service](https://www.twilio.com/docs/messaging/api/service-resource)
                this conversation belongs to.
            state (ConversationState, optional): Current state of this conversation.
                Can be either `initializing`, `active`, `inactive` or `closed` and
                defaults to `active`
            timers_inactive (str, optional): ISO8601 duration when conversation will
                be switched to `inactive` state. Minimum value for this timer is 1
                minute.
            timers_closed (str, optional): ISO8601 duration when conversation will be
                switched to `closed` state. Minimum value for this timer is 10
                minutes.
            unique_name (str, optional): An application-defined string that uniquely
                identifies the resource. It can be used to address the resource in
                place of the resource's `sid` in the URL.
            bindings_email_address (str, optional): The default email address that
                will be used when sending outbound emails in this conversation.
            bindings_email_name (str, optional): The default name that will be used
                when sending outbound emails in this conversation.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("FriendlyName")
                .value(friendly_name))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MessagingServiceSid")
                .value(messaging_service_sid))
            .form_param(Parameter()
                .key("State")
                .value(state))
            .form_param(Parameter()
                .key("Timers.Inactive")
                .value(timers_inactive))
            .form_param(Parameter()
                .key("Timers.Closed")
                .value(timers_closed))
            .form_param(Parameter()
                .key("UniqueName")
                .value(unique_name))
            .form_param(Parameter()
                .key("Bindings.Email.Address")
                .value(bindings_email_address))
            .form_param(Parameter()
                .key("Bindings.Email.Name")
                .value(bindings_email_name))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Conversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_conversation(self,
                            sid,
                            x_twilio_webhook_enabled=None):
        """Perform a DELETE request to /v1/Conversations/{Sid}.

        Remove a conversation from your account's default service

        Args:
            sid (str): A 34 character string that uniquely identifies this resource.
                Can also be the `unique_name` of the Conversation.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_conversation(self,
                           sid):
        """Perform a GET request to /v1/Conversations/{Sid}.

        Fetch a conversation from your account's default service

        Args:
            sid (str): A 34 character string that uniquely identifies this resource.
                Can also be the `unique_name` of the Conversation.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Conversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def create_service_conversation(self,
                                    chat_service_sid,
                                    x_twilio_webhook_enabled=None,
                                    friendly_name=None,
                                    unique_name=None,
                                    attributes=None,
                                    messaging_service_sid=None,
                                    date_created=None,
                                    date_updated=None,
                                    state=None,
                                    timers_inactive=None,
                                    timers_closed=None,
                                    bindings_email_address=None,
                                    bindings_email_name=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations.

        Create a new conversation in your service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            friendly_name (str, optional): The human-readable name of this
                conversation, limited to 256 characters. Optional.
            unique_name (str, optional): An application-defined string that uniquely
                identifies the resource. It can be used to address the resource in
                place of the resource's `sid` in the URL.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set "{}" will be returned.
            messaging_service_sid (str, optional): The unique ID of the [Messaging
                Service](https://www.twilio.com/docs/messaging/api/service-resource)
                this conversation belongs to.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated.
            state (ServiceConversationState, optional): Current state of this
                conversation. Can be either `initializing`, `active`, `inactive` or
                `closed` and defaults to `active`
            timers_inactive (str, optional): ISO8601 duration when conversation will
                be switched to `inactive` state. Minimum value for this timer is 1
                minute.
            timers_closed (str, optional): ISO8601 duration when conversation will be
                switched to `closed` state. Minimum value for this timer is 10
                minutes.
            bindings_email_address (str, optional): The default email address that
                will be used when sending outbound emails in this conversation.
            bindings_email_name (str, optional): The default name that will be used
                when sending outbound emails in this conversation.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("FriendlyName")
                .value(friendly_name))
            .form_param(Parameter()
                .key("UniqueName")
                .value(unique_name))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MessagingServiceSid")
                .value(messaging_service_sid))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("State")
                .value(state))
            .form_param(Parameter()
                .key("Timers.Inactive")
                .value(timers_inactive))
            .form_param(Parameter()
                .key("Timers.Closed")
                .value(timers_closed))
            .form_param(Parameter()
                .key("Bindings.Email.Address")
                .value(bindings_email_address))
            .form_param(Parameter()
                .key("Bindings.Email.Name")
                .value(bindings_email_name))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_service_conversation(self,
                                  chat_service_sid,
                                  start_date=None,
                                  end_date=None,
                                  state=None,
                                  page_size=None,
                                  page=None,
                                  page_token=None):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations.

        Retrieve a list of conversations in your service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            start_date (str, optional): Specifies the beginning of the date range for
                filtering Conversations based on their creation date. Conversations
                that were created on or after this date will be included in the
                results. The date must be in ISO8601 format, specifically starting at
                the beginning of the specified date (YYYY-MM-DDT00:00:00Z), for
                precise filtering. This parameter can be combined with other filters.
                If this filter is used, the returned list is sorted by latest
                conversation creation date in descending order.
            end_date (str, optional): Defines the end of the date range for filtering
                conversations by their creation date. Only conversations that were
                created on or before this date will appear in the results.  The date
                must be in ISO8601 format, specifically capturing up to the end of
                the specified date (YYYY-MM-DDT23:59:59Z), to ensure that
                conversations from the entire end day are included. This parameter
                can be combined with other filters. If this filter is used, the
                returned list is sorted by latest conversation creation date in
                descending order.
            state (ServiceConversationState, optional): State for sorting and
                filtering list of Conversations. Can be `active`, `inactive` or
                `closed`
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 100.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("StartDate")
                .value(start_date))
            .query_param(Parameter()
                .key("EndDate")
                .value(end_date))
            .query_param(Parameter()
                .key("State")
                .value(state))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListServiceConversationResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_service_conversation(self,
                                    chat_service_sid,
                                    sid,
                                    x_twilio_webhook_enabled=None,
                                    friendly_name=None,
                                    date_created=None,
                                    date_updated=None,
                                    attributes=None,
                                    messaging_service_sid=None,
                                    state=None,
                                    timers_inactive=None,
                                    timers_closed=None,
                                    unique_name=None,
                                    bindings_email_address=None,
                                    bindings_email_name=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{Sid}.

        Update an existing conversation in your service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            sid (str): A 34 character string that uniquely identifies this resource.
                Can also be the `unique_name` of the Conversation.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            friendly_name (str, optional): The human-readable name of this
                conversation, limited to 256 characters. Optional.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated.
            attributes (str, optional): An optional string metadata field you can use
                to store any data you wish. The string value must contain
                structurally valid JSON if specified.  **Note** that if the
                attributes are not set "{}" will be returned.
            messaging_service_sid (str, optional): The unique ID of the [Messaging
                Service](https://www.twilio.com/docs/messaging/api/service-resource)
                this conversation belongs to.
            state (ServiceConversationState, optional): Current state of this
                conversation. Can be either `initializing`, `active`, `inactive` or
                `closed` and defaults to `active`
            timers_inactive (str, optional): ISO8601 duration when conversation will
                be switched to `inactive` state. Minimum value for this timer is 1
                minute.
            timers_closed (str, optional): ISO8601 duration when conversation will be
                switched to `closed` state. Minimum value for this timer is 10
                minutes.
            unique_name (str, optional): An application-defined string that uniquely
                identifies the resource. It can be used to address the resource in
                place of the resource's `sid` in the URL.
            bindings_email_address (str, optional): The default email address that
                will be used when sending outbound emails in this conversation.
            bindings_email_name (str, optional): The default name that will be used
                when sending outbound emails in this conversation.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("FriendlyName")
                .value(friendly_name))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MessagingServiceSid")
                .value(messaging_service_sid))
            .form_param(Parameter()
                .key("State")
                .value(state))
            .form_param(Parameter()
                .key("Timers.Inactive")
                .value(timers_inactive))
            .form_param(Parameter()
                .key("Timers.Closed")
                .value(timers_closed))
            .form_param(Parameter()
                .key("UniqueName")
                .value(unique_name))
            .form_param(Parameter()
                .key("Bindings.Email.Address")
                .value(bindings_email_address))
            .form_param(Parameter()
                .key("Bindings.Email.Name")
                .value(bindings_email_name))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_service_conversation(self,
                                    chat_service_sid,
                                    sid,
                                    x_twilio_webhook_enabled=None):
        """Perform a DELETE request to
        /v1/Services/{ChatServiceSid}/Conversations/{Sid}.

        Remove a conversation from your service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            sid (str): A 34 character string that uniquely identifies this resource.
                Can also be the `unique_name` of the Conversation.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_service_conversation(self,
                                   chat_service_sid,
                                   sid):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{Sid}.

        Fetch a conversation from your service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            sid (str): A 34 character string that uniquely identifies this resource.
                Can also be the `unique_name` of the Conversation.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversation.from_dictionary)
            .is_api_response(True),
        ).execute()
