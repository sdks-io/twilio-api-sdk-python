"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from twilioapis.api_helper import APIHelper
from twilioapis.apis.base_api import BaseApi
from twilioapis.configuration import Server
from twilioapis.http.http_method_enum import (
    HttpMethodEnum,
)
from twilioapis.models.conversation_message import (
    ConversationMessage,
)
from twilioapis.models.list_conversation_message_response import (
    ListConversationMessageResponse,
)
from twilioapis.models.list_service_conversation_message_response import (
    ListServiceConversationMessageResponse,
)
from twilioapis.models.service_conversation_message import (
    ServiceConversationMessage,
)


class ConversationsV1MessageApi(BaseApi):
    """A Controller to access Endpoints in the twilioapis API."""

    def __init__(self, config):
        """Initialize ConversationsV1MessageApi object."""
        super(ConversationsV1MessageApi, self).__init__(config)

    def create_conversation_message(self,
                                    conversation_sid,
                                    x_twilio_webhook_enabled=None,
                                    author=None,
                                    body=None,
                                    date_created=None,
                                    date_updated=None,
                                    attributes=None,
                                    media_sid=None,
                                    content_sid=None,
                                    content_variables=None,
                                    subject=None):
        """Perform a POST request to
        /v1/Conversations/{ConversationSid}/Messages.

        Add a new message to the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            author (str, optional): The channel specific identifier of the message's
                author. Defaults to `system`.
            body (str, optional): The content of the message, can be up to 1,600
                characters long.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated. `null` if the message has not been edited.
            attributes (str, optional): A string metadata field you can use to store
                any data you wish. The string value must contain structurally valid
                JSON if specified.  **Note** that if the attributes are not set "{}"
                will be returned.
            media_sid (str, optional): The Media SID to be attached to the new
                Message.
            content_sid (str, optional): The unique ID of the multi-channel [Rich
                Content](https://www.twilio.com/docs/content) template, required for
                template-generated messages.  **Note** that if this field is set,
                `Body` and `MediaSid` parameters are ignored.
            content_variables (str, optional): A structurally valid JSON string that
                contains values to resolve Rich Content template variables.
            subject (str, optional): The subject of the message, can be up to 256
                characters long.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Messages")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("Author")
                .value(author))
            .form_param(Parameter()
                .key("Body")
                .value(body))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MediaSid")
                .value(media_sid))
            .form_param(Parameter()
                .key("ContentSid")
                .value(content_sid))
            .form_param(Parameter()
                .key("ContentVariables")
                .value(content_variables))
            .form_param(Parameter()
                .key("Subject")
                .value(subject))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationMessage.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_conversation_message(self,
                                  conversation_sid,
                                  order=None,
                                  page_size=None,
                                  page=None,
                                  page_token=None):
        """Perform a GET request to
        /v1/Conversations/{ConversationSid}/Messages.

        Retrieve a list of all messages in the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for messages.
            order (ConversationMessageOrderType, optional): The sort order of the
                returned messages. Can be: `asc` (ascending) or `desc` (descending),
                with `asc` as the default.
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 100.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Messages")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("Order")
                .value(order))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListConversationMessageResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_conversation_message(self,
                                    conversation_sid,
                                    sid,
                                    x_twilio_webhook_enabled=None,
                                    author=None,
                                    body=None,
                                    date_created=None,
                                    date_updated=None,
                                    attributes=None,
                                    subject=None):
        """Perform a POST request to
        /v1/Conversations/{ConversationSid}/Messages/{Sid}.

        Update an existing message in the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            author (str, optional): The channel specific identifier of the message's
                author. Defaults to `system`.
            body (str, optional): The content of the message, can be up to 1,600
                characters long.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated. `null` if the message has not been edited.
            attributes (str, optional): A string metadata field you can use to store
                any data you wish. The string value must contain structurally valid
                JSON if specified.  **Note** that if the attributes are not set "{}"
                will be returned.
            subject (str, optional): The subject of the message, can be up to 256
                characters long.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Messages/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("Author")
                .value(author))
            .form_param(Parameter()
                .key("Body")
                .value(body))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("Subject")
                .value(subject))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationMessage.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_conversation_message(self,
                                    conversation_sid,
                                    sid,
                                    x_twilio_webhook_enabled=None):
        """Perform a DELETE request to
        /v1/Conversations/{ConversationSid}/Messages/{Sid}.

        Remove a message from the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Messages/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_conversation_message(self,
                                   conversation_sid,
                                   sid):
        """Perform a GET request to
        /v1/Conversations/{ConversationSid}/Messages/{Sid}.

        Fetch a message from the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            sid (str): A 34 character string that uniquely identifies this resource.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Messages/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationMessage.from_dictionary)
            .is_api_response(True),
        ).execute()

    def create_service_conversation_message(self,
                                            chat_service_sid,
                                            conversation_sid,
                                            x_twilio_webhook_enabled=None,
                                            author=None,
                                            body=None,
                                            date_created=None,
                                            date_updated=None,
                                            attributes=None,
                                            media_sid=None,
                                            content_sid=None,
                                            content_variables=None,
                                            subject=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages.

        Add a new message to the conversation in a specific service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            author (str, optional): The channel specific identifier of the message's
                author. Defaults to `system`.
            body (str, optional): The content of the message, can be up to 1,600
                characters long.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated. `null` if the message has not been edited.
            attributes (str, optional): A string metadata field you can use to store
                any data you wish. The string value must contain structurally valid
                JSON if specified.  **Note** that if the attributes are not set "{}"
                will be returned.
            media_sid (str, optional): The Media SID to be attached to the new
                Message.
            content_sid (str, optional): The unique ID of the multi-channel [Rich
                Content](https://www.twilio.com/docs/content) template, required for
                template-generated messages.  **Note** that if this field is set,
                `Body` and `MediaSid` parameters are ignored.
            content_variables (str, optional): A structurally valid JSON string that
                contains values to resolve Rich Content template variables.
            subject (str, optional): The subject of the message, can be up to 256
                characters long.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("Author")
                .value(author))
            .form_param(Parameter()
                .key("Body")
                .value(body))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("MediaSid")
                .value(media_sid))
            .form_param(Parameter()
                .key("ContentSid")
                .value(content_sid))
            .form_param(Parameter()
                .key("ContentVariables")
                .value(content_variables))
            .form_param(Parameter()
                .key("Subject")
                .value(subject))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationMessage.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_service_conversation_message(self,
                                          chat_service_sid,
                                          conversation_sid,
                                          order=None,
                                          page_size=None,
                                          page=None,
                                          page_token=None):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages.

        Retrieve a list of all messages in the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for messages.
            order (ServiceConversationMessageOrderType, optional): The sort order of
                the returned messages. Can be: `asc` (ascending) or `desc`
                (descending), with `asc` as the default.
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 100.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("Order")
                .value(order))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListServiceConversationMessageResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_service_conversation_message(self,
                                            chat_service_sid,
                                            conversation_sid,
                                            sid,
                                            x_twilio_webhook_enabled=None,
                                            author=None,
                                            body=None,
                                            date_created=None,
                                            date_updated=None,
                                            attributes=None,
                                            subject=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}.

        Update an existing message in the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header
            author (str, optional): The channel specific identifier of the message's
                author. Defaults to `system`.
            body (str, optional): The content of the message, can be up to 1,600
                characters long.
            date_created (datetime, optional): The date that this resource was
                created.
            date_updated (datetime, optional): The date that this resource was last
                updated. `null` if the message has not been edited.
            attributes (str, optional): A string metadata field you can use to store
                any data you wish. The string value must contain structurally valid
                JSON if specified.  **Note** that if the attributes are not set "{}"
                will be returned.
            subject (str, optional): The subject of the message, can be up to 256
                characters long.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .form_param(Parameter()
                .key("Author")
                .value(author))
            .form_param(Parameter()
                .key("Body")
                .value(body))
            .form_param(Parameter()
                .key("DateCreated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_created)))
            .form_param(Parameter()
                .key("DateUpdated")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    date_updated)))
            .form_param(Parameter()
                .key("Attributes")
                .value(attributes))
            .form_param(Parameter()
                .key("Subject")
                .value(subject))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationMessage.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_service_conversation_message(self,
                                            chat_service_sid,
                                            conversation_sid,
                                            sid,
                                            x_twilio_webhook_enabled=None):
        """Perform a DELETE request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}.

        Remove a message from the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            sid (str): A 34 character string that uniquely identifies this resource.
            x_twilio_webhook_enabled (ChannelWebhookEnabledType, optional): The
                X-Twilio-Webhook-Enabled HTTP request header

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("X-Twilio-Webhook-Enabled")
                .value(x_twilio_webhook_enabled))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_service_conversation_message(self,
                                           chat_service_sid,
                                           conversation_sid,
                                           sid):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}.

        Fetch a message from the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this message.
            sid (str): A 34 character string that uniquely identifies this resource.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Messages/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationMessage.from_dictionary)
            .is_api_response(True),
        ).execute()
