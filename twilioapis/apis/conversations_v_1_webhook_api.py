"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from twilioapis.api_helper import APIHelper
from twilioapis.apis.base_api import BaseApi
from twilioapis.configuration import Server
from twilioapis.http.http_method_enum import (
    HttpMethodEnum,
)
from twilioapis.models.configuration_webhook import (
    ConfigurationWebhook,
)
from twilioapis.models.conversation_scoped_webhook import (
    ConversationScopedWebhook,
)
from twilioapis.models.list_conversation_scoped_webhook_response import (
    ListConversationScopedWebhookResponse,
)
from twilioapis.models.list_service_conversation_scoped_webhook_response import (
    ListServiceConversationScopedWebhookResponse,
)
from twilioapis.models.service_conversation_scoped_webhook import (
    ServiceConversationScopedWebhook,
)
from twilioapis.models.service_webhook_configuration import (
    ServiceWebhookConfiguration,
)


class ConversationsV1WebhookApi(BaseApi):
    """A Controller to access Endpoints in the twilioapis API."""

    def __init__(self, config):
        """Initialize ConversationsV1WebhookApi object."""
        super(ConversationsV1WebhookApi, self).__init__(config)

    def fetch_configuration_webhook(self):
        """Perform a GET request to /v1/Configuration/Webhooks.

        A Webhook resource manages a service-level set of callback URLs and their
        configuration for receiving all conversation events.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Configuration/Webhooks")
            .http_method(HttpMethodEnum.GET)
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConfigurationWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_configuration_webhook(self,
                                     method=None,
                                     filters=None,
                                     pre_webhook_url=None,
                                     post_webhook_url=None,
                                     target=None):
        """Perform a POST request to /v1/Configuration/Webhooks.

        A Webhook resource manages a service-level set of callback URLs and their
        configuration for receiving all conversation events.

        Args:
            method (str, optional): The HTTP method to be used when sending a webhook
                request.
            filters (List[str], optional): The list of webhook event triggers that
                are enabled for this Service: `onMessageAdded`, `onMessageUpdated`,
                `onMessageRemoved`, `onMessageAdd`, `onMessageUpdate`,
                `onMessageRemove`, `onConversationUpdated`, `onConversationRemoved`,
                `onConversationAdd`, `onConversationAdded`, `onConversationRemove`,
                `onConversationUpdate`, `onConversationStateUpdated`,
                `onParticipantAdded`, `onParticipantUpdated`, `onParticipantRemoved`,
                `onParticipantAdd`, `onParticipantRemove`, `onParticipantUpdate`,
                `onDeliveryUpdated`, `onUserAdded`, `onUserUpdate`, `onUserUpdated`
            pre_webhook_url (str, optional): The absolute url the pre-event webhook
                request should be sent to.
            post_webhook_url (str, optional): The absolute url the post-event webhook
                request should be sent to.
            target (ConfigurationWebhookTarget, optional): The routing target of the
                webhook. Can be ordinary or route internally to Flex

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Configuration/Webhooks")
            .http_method(HttpMethodEnum.POST)
            .form_param(Parameter()
                .key("Method")
                .value(method))
            .form_param(Parameter()
                .key("Filters")
                .value(filters))
            .form_param(Parameter()
                .key("PreWebhookUrl")
                .value(pre_webhook_url))
            .form_param(Parameter()
                .key("PostWebhookUrl")
                .value(post_webhook_url))
            .form_param(Parameter()
                .key("Target")
                .value(target))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConfigurationWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_conversation_scoped_webhook(self,
                                         conversation_sid,
                                         page_size=None,
                                         page=None,
                                         page_token=None):
        """Perform a GET request to
        /v1/Conversations/{ConversationSid}/Webhooks.

        Retrieve a list of all webhooks scoped to the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            page_size (int, optional): How many resources to return in each list
                page. The default is 5, and the maximum is 5.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Webhooks")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListConversationScopedWebhookResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def create_conversation_scoped_webhook(self,
                                           conversation_sid,
                                           target,
                                           configuration_url=None,
                                           configuration_method=None,
                                           configuration_filters=None,
                                           configuration_triggers=None,
                                           configuration_flow_sid=None,
                                           configuration_replay_after=None):
        """Perform a POST request to
        /v1/Conversations/{ConversationSid}/Webhooks.

        Create a new webhook scoped to the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            target (ConversationScopedWebhookTarget): The target of this webhook:
                `webhook`, `studio`, `trigger`
            configuration_url (str, optional): The absolute url the webhook request
                should be sent to.
            configuration_method (ConversationScopedWebhookMethod, optional): The
                request form parameter.
            configuration_filters (List[str], optional): The list of events, firing
                webhook event for this Conversation.
            configuration_triggers (List[str], optional): The list of keywords,
                firing webhook event for this Conversation.
            configuration_flow_sid (str, optional): The studio flow SID, where the
                webhook should be sent to.
            configuration_replay_after (int, optional): The message index for which
                and it's successors the webhook will be replayed. Not set by default

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Webhooks")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("Target")
                .value(target)
                .is_required(True))
            .form_param(Parameter()
                .key("Configuration.Url")
                .value(configuration_url))
            .form_param(Parameter()
                .key("Configuration.Method")
                .value(configuration_method))
            .form_param(Parameter()
                .key("Configuration.Filters")
                .value(configuration_filters))
            .form_param(Parameter()
                .key("Configuration.Triggers")
                .value(configuration_triggers))
            .form_param(Parameter()
                .key("Configuration.FlowSid")
                .value(configuration_flow_sid))
            .form_param(Parameter()
                .key("Configuration.ReplayAfter")
                .value(configuration_replay_after))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationScopedWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def fetch_conversation_scoped_webhook(self,
                                          conversation_sid,
                                          sid):
        """Perform a GET request to
        /v1/Conversations/{ConversationSid}/Webhooks/{Sid}.

        Fetch the configuration of a conversation-scoped webhook

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            sid (str): A 34 character string that uniquely identifies this resource.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Webhooks/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationScopedWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_conversation_scoped_webhook(self,
                                           conversation_sid,
                                           sid,
                                           configuration_url=None,
                                           configuration_method=None,
                                           configuration_filters=None,
                                           configuration_triggers=None,
                                           configuration_flow_sid=None):
        """Perform a POST request to
        /v1/Conversations/{ConversationSid}/Webhooks/{Sid}.

        Update an existing conversation-scoped webhook

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            sid (str): A 34 character string that uniquely identifies this resource.
            configuration_url (str, optional): The absolute url the webhook request
                should be sent to.
            configuration_method (ConversationScopedWebhookMethod, optional): The
                request form parameter.
            configuration_filters (List[str], optional): The list of events, firing
                webhook event for this Conversation.
            configuration_triggers (List[str], optional): The list of keywords,
                firing webhook event for this Conversation.
            configuration_flow_sid (str, optional): The studio flow SID, where the
                webhook should be sent to.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Webhooks/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("Configuration.Url")
                .value(configuration_url))
            .form_param(Parameter()
                .key("Configuration.Method")
                .value(configuration_method))
            .form_param(Parameter()
                .key("Configuration.Filters")
                .value(configuration_filters))
            .form_param(Parameter()
                .key("Configuration.Triggers")
                .value(configuration_triggers))
            .form_param(Parameter()
                .key("Configuration.FlowSid")
                .value(configuration_flow_sid))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ConversationScopedWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_conversation_scoped_webhook(self,
                                           conversation_sid,
                                           sid):
        """Perform a DELETE request to
        /v1/Conversations/{ConversationSid}/Webhooks/{Sid}.

        Remove an existing webhook scoped to the conversation

        Args:
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            sid (str): A 34 character string that uniquely identifies this resource.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Conversations/{ConversationSid}/Webhooks/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def create_service_conversation_scoped_webhook(self,
                                                   chat_service_sid,
                                                   conversation_sid,
                                                   target,
                                                   configuration_url=None,
                                                   configuration_method=None,
                                                   configuration_filters=None,
                                                   configuration_triggers=None,
                                                   configuration_flow_sid=None,
                                                   configuration_replay_after=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks.

        Create a new webhook scoped to the conversation in a specific service

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            target (ServiceConversationScopedWebhookTarget): The target of this
                webhook: `webhook`, `studio`, `trigger`
            configuration_url (str, optional): The absolute url the webhook request
                should be sent to.
            configuration_method (ServiceConversationScopedWebhookMethod, optional):
                The request form parameter.
            configuration_filters (List[str], optional): The list of events, firing
                webhook event for this Conversation.
            configuration_triggers (List[str], optional): The list of keywords,
                firing webhook event for this Conversation.
            configuration_flow_sid (str, optional): The studio flow SID, where the
                webhook should be sent to.
            configuration_replay_after (int, optional): The message index for which
                and it's successors the webhook will be replayed. Not set by default

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Created

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("Target")
                .value(target)
                .is_required(True))
            .form_param(Parameter()
                .key("Configuration.Url")
                .value(configuration_url))
            .form_param(Parameter()
                .key("Configuration.Method")
                .value(configuration_method))
            .form_param(Parameter()
                .key("Configuration.Filters")
                .value(configuration_filters))
            .form_param(Parameter()
                .key("Configuration.Triggers")
                .value(configuration_triggers))
            .form_param(Parameter()
                .key("Configuration.FlowSid")
                .value(configuration_flow_sid))
            .form_param(Parameter()
                .key("Configuration.ReplayAfter")
                .value(configuration_replay_after))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationScopedWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_service_conversation_scoped_webhook(self,
                                                 chat_service_sid,
                                                 conversation_sid,
                                                 page_size=None,
                                                 page=None,
                                                 page_token=None):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks.

        Retrieve a list of all webhooks scoped to the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            page_size (int, optional): How many resources to return in each list
                page. The default is 5, and the maximum is 5.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListServiceConversationScopedWebhookResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_service_conversation_scoped_webhook(self,
                                                   chat_service_sid,
                                                   conversation_sid,
                                                   sid,
                                                   configuration_url=None,
                                                   configuration_method=None,
                                                   configuration_filters=None,
                                                   configuration_triggers=None,
                                                   configuration_flow_sid=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}.

        Update an existing conversation-scoped webhook

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            sid (str): A 34 character string that uniquely identifies this resource.
            configuration_url (str, optional): The absolute url the webhook request
                should be sent to.
            configuration_method (ServiceConversationScopedWebhookMethod, optional):
                The request form parameter.
            configuration_filters (List[str], optional): The list of events, firing
                webhook event for this Conversation.
            configuration_triggers (List[str], optional): The list of keywords,
                firing webhook event for this Conversation.
            configuration_flow_sid (str, optional): The studio flow SID, where the
                webhook should be sent to.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("Configuration.Url")
                .value(configuration_url))
            .form_param(Parameter()
                .key("Configuration.Method")
                .value(configuration_method))
            .form_param(Parameter()
                .key("Configuration.Filters")
                .value(configuration_filters))
            .form_param(Parameter()
                .key("Configuration.Triggers")
                .value(configuration_triggers))
            .form_param(Parameter()
                .key("Configuration.FlowSid")
                .value(configuration_flow_sid))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationScopedWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_service_conversation_scoped_webhook(self,
                                                   chat_service_sid,
                                                   conversation_sid,
                                                   sid):
        """Perform a DELETE request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}.

        Remove an existing webhook scoped to the conversation

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            sid (str): A 34 character string that uniquely identifies this resource.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_service_conversation_scoped_webhook(self,
                                                  chat_service_sid,
                                                  conversation_sid,
                                                  sid):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}.

        Fetch the configuration of a conversation-scoped webhook

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Participant resource is associated with.
            conversation_sid (str): The unique ID of the
                [Conversation](https://www.twilio.com/docs/conversations/api/conversat
                ion-resource) for this webhook.
            sid (str): A 34 character string that uniquely identifies this resource.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Conversations/{ConversationSid}/Webhooks/{Sid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("Sid")
                .value(sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceConversationScopedWebhook.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_service_webhook_configuration(self,
                                             chat_service_sid,
                                             pre_webhook_url=None,
                                             post_webhook_url=None,
                                             filters=None,
                                             method=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Configuration/Webhooks.

        Update a specific Webhook.

        Args:
            chat_service_sid (str): The unique ID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) this conversation belongs to.
            pre_webhook_url (str, optional): The absolute url the pre-event webhook
                request should be sent to.
            post_webhook_url (str, optional): The absolute url the post-event webhook
                request should be sent to.
            filters (List[str], optional): The list of events that your configured
                webhook targets will receive. Events not configured here will not
                fire. Possible values are `onParticipantAdd`, `onParticipantAdded`,
                `onDeliveryUpdated`, `onConversationUpdated`, `onConversationRemove`,
                `onParticipantRemove`, `onConversationUpdate`, `onMessageAdd`,
                `onMessageRemoved`, `onParticipantUpdated`, `onConversationAdded`,
                `onMessageAdded`, `onConversationAdd`, `onConversationRemoved`,
                `onParticipantUpdate`, `onMessageRemove`, `onMessageUpdated`,
                `onParticipantRemoved`, `onMessageUpdate` or
                `onConversationStateUpdated`.
            method (str, optional): The HTTP method to be used when sending a webhook
                request. One of `GET` or `POST`.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Configuration/Webhooks")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("PreWebhookUrl")
                .value(pre_webhook_url))
            .form_param(Parameter()
                .key("PostWebhookUrl")
                .value(post_webhook_url))
            .form_param(Parameter()
                .key("Filters")
                .value(filters))
            .form_param(Parameter()
                .key("Method")
                .value(method))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceWebhookConfiguration.from_dictionary)
            .is_api_response(True),
        ).execute()

    def fetch_service_webhook_configuration(self,
                                            chat_service_sid):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Configuration/Webhooks.

        Fetch a specific service webhook configuration.

        Args:
            chat_service_sid (str): The unique ID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) this conversation belongs to.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Configuration/Webhooks")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceWebhookConfiguration.from_dictionary)
            .is_api_response(True),
        ).execute()
