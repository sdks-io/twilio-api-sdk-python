"""twilioapis.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from twilioapis.api_helper import APIHelper
from twilioapis.apis.base_api import BaseApi
from twilioapis.configuration import Server
from twilioapis.http.http_method_enum import (
    HttpMethodEnum,
)
from twilioapis.models.list_service_user_conversation_response import (
    ListServiceUserConversationResponse,
)
from twilioapis.models.list_user_conversation_response import (
    ListUserConversationResponse,
)
from twilioapis.models.service_user_conversation import (
    ServiceUserConversation,
)
from twilioapis.models.user_conversation import (
    UserConversation,
)


class ConversationsV1UserConversationApi(BaseApi):
    """A Controller to access Endpoints in the twilioapis API."""

    def __init__(self, config):
        """Initialize ConversationsV1UserConversationApi object."""
        super(ConversationsV1UserConversationApi, self).__init__(config)

    def update_service_user_conversation(self,
                                         chat_service_sid,
                                         user_sid,
                                         conversation_sid,
                                         notification_level=None,
                                         last_read_timestamp=None,
                                         last_read_message_index=None):
        """Perform a POST request to
        /v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations/{ConversationSid}.

        Update a specific User Conversation.

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            conversation_sid (str): The unique SID identifier of the Conversation.
                This value can be either the `sid` or the `unique_name` of the
                [Conversation
                resource](https://www.twilio.com/docs/conversations/api/conversation-r
                esource).
            notification_level (ServiceUserConversationNotificationLevel, optional):
                The Notification Level of this User Conversation. One of `default` or
                `muted`.
            last_read_timestamp (datetime, optional): The date of the last message
                read in conversation by the user, given in ISO 8601 format.
            last_read_message_index (int, optional): The index of the last Message in
                the Conversation that the Participant has read.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations/{ConversationSid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("NotificationLevel")
                .value(notification_level))
            .form_param(Parameter()
                .key("LastReadTimestamp")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    last_read_timestamp)))
            .form_param(Parameter()
                .key("LastReadMessageIndex")
                .value(last_read_message_index))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceUserConversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_service_user_conversation(self,
                                         chat_service_sid,
                                         user_sid,
                                         conversation_sid):
        """Perform a DELETE request to
        /v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations/{ConversationSid}.

        Delete a specific User Conversation.

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            conversation_sid (str): The unique SID identifier of the Conversation.
                This value can be either the `sid` or the `unique_name` of the
                [Conversation
                resource](https://www.twilio.com/docs/conversations/api/conversation-r
                esource).

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations/{ConversationSid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_service_user_conversation(self,
                                        chat_service_sid,
                                        user_sid,
                                        conversation_sid):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations/{ConversationSid}.

        Fetch a specific User Conversation.

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            conversation_sid (str): The unique SID identifier of the Conversation.
                This value can be either the `sid` or the `unique_name` of the
                [Conversation
                resource](https://www.twilio.com/docs/conversations/api/conversation-r
                esource).

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations/{ConversationSid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ServiceUserConversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_service_user_conversation(self,
                                       chat_service_sid,
                                       user_sid,
                                       page_size=None,
                                       page=None,
                                       page_token=None):
        """Perform a GET request to
        /v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations.

        Retrieve a list of all User Conversations for the User.

        Args:
            chat_service_sid (str): The SID of the [Conversation
                Service](https://www.twilio.com/docs/conversations/api/service-resourc
                e) the Conversation resource is associated with.
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 50.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Services/{ChatServiceSid}/Users/{UserSid}/Conversations")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("ChatServiceSid")
                .value(chat_service_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListServiceUserConversationResponse.from_dictionary)
            .is_api_response(True),
        ).execute()

    def update_user_conversation(self,
                                 user_sid,
                                 conversation_sid,
                                 notification_level=None,
                                 last_read_timestamp=None,
                                 last_read_message_index=None):
        """Perform a POST request to
        /v1/Users/{UserSid}/Conversations/{ConversationSid}.

        Update a specific User Conversation.

        Args:
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            conversation_sid (str): The unique SID identifier of the Conversation.
                This value can be either the `sid` or the `unique_name` of the
                [Conversation
                resource](https://www.twilio.com/docs/conversations/api/conversation-r
                esource).
            notification_level (UserConversationNotificationLevel, optional): The
                Notification Level of this User Conversation. One of `default` or
                `muted`.
            last_read_timestamp (datetime, optional): The date of the last message
                read in conversation by the user, given in ISO 8601 format.
            last_read_message_index (int, optional): The index of the last Message in
                the Conversation that the Participant has read.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Users/{UserSid}/Conversations/{ConversationSid}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .form_param(Parameter()
                .key("NotificationLevel")
                .value(notification_level))
            .form_param(Parameter()
                .key("LastReadTimestamp")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    last_read_timestamp)))
            .form_param(Parameter()
                .key("LastReadMessageIndex")
                .value(last_read_message_index))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(UserConversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def delete_user_conversation(self,
                                 user_sid,
                                 conversation_sid):
        """Perform a DELETE request to
        /v1/Users/{UserSid}/Conversations/{ConversationSid}.

        Delete a specific User Conversation.

        Args:
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            conversation_sid (str): The unique SID identifier of the Conversation.
                This value can be either the `sid` or the `unique_name` of the
                [Conversation
                resource](https://www.twilio.com/docs/conversations/api/conversation-r
                esource).

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. The resource was
                deleted successfully.

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Users/{UserSid}/Conversations/{ConversationSid}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .is_api_response(True),
        ).execute()

    def fetch_user_conversation(self,
                                user_sid,
                                conversation_sid):
        """Perform a GET request to
        /v1/Users/{UserSid}/Conversations/{ConversationSid}.

        Fetch a specific User Conversation.

        Args:
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            conversation_sid (str): The unique SID identifier of the Conversation.
                This value can be either the `sid` or the `unique_name` of the
                [Conversation
                resource](https://www.twilio.com/docs/conversations/api/conversation-r
                esource).

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Users/{UserSid}/Conversations/{ConversationSid}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("ConversationSid")
                .value(conversation_sid)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(UserConversation.from_dictionary)
            .is_api_response(True),
        ).execute()

    def list_user_conversation(self,
                               user_sid,
                               page_size=None,
                               page=None,
                               page_token=None):
        """Perform a GET request to /v1/Users/{UserSid}/Conversations.

        Retrieve a list of all User Conversations for the User.

        Args:
            user_sid (str): The unique SID identifier of the [User
                resource](https://www.twilio.com/docs/conversations/api/user-resource)
                . This value can be either the `sid` or the `identity` of the User
                resource.
            page_size (int, optional): How many resources to return in each list
                page. The default is 50, and the maximum is 50.
            page (int, optional): The page index. This value is simply for client
                state.
            page_token (str, optional): The page token. This is provided by the API.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. OK

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT2)
            .path("/v1/Users/{UserSid}/Conversations")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("UserSid")
                .value(user_sid)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("PageSize")
                .value(page_size))
            .query_param(Parameter()
                .key("Page")
                .value(page))
            .query_param(Parameter()
                .key("PageToken")
                .value(page_token))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("accountSid_authToken")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListUserConversationResponse.from_dictionary)
            .is_api_response(True),
        ).execute()
